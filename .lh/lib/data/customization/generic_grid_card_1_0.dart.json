{
    "sourceFile": "lib/data/customization/generic_grid_card_1_0.dart",
    "activeCommit": 0,
    "commits": [
        {
            "activePatchIndex": 12,
            "patches": [
                {
                    "date": 1759781203371,
                    "content": "Index: \n===================================================================\n--- \n+++ \n"
                },
                {
                    "date": 1759781252468,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,260 @@\n+import 'package:file_picker/file_picker.dart';\r\n+import 'package:flutter/material.dart';\r\n+import 'package:intl/intl.dart';\r\n+import 'package:shared_preferences/shared_preferences.dart';\r\n+import 'package:task_manager_flutter/data/models/network_response.dart';\r\n+import 'package:task_manager_flutter/data/services/network_caller.dart';\r\n+import 'package:task_manager_flutter/data/services/upload_file_caller.dart';\r\n+import 'package:task_manager_flutter/ui/widgets/user_banners.dart';\r\n+import 'package:task_manager_flutter/data/customization/generic_grid_card.dart';\r\n+import '../models/telas_model.dart';\r\n+// ==============================================\r\n+// MOBILE GRID SCREEN - MATERIAL DESIGN 3 COMPLETO\r\n+// ==============================================\r\n+\r\n+// screens/dynamic_grid_screen.dart\r\n+class DynamicGridScreen<T> extends StatefulWidget {\r\n+  final String telaNome;\r\n+  final FromJson<T> fromJson;\r\n+  final ToJson<T> toJson;\r\n+  final SecurityCheck hasPermission;\r\n+  final String? storageKey;\r\n+  final OnItemTap<T>? onItemTap;\r\n+  final Widget Function(T item)? detailScreenBuilder;\r\n+  final Map<String, dynamic>? extraParams;\r\n+  final VoidCallback? onUserBannerTapped;\r\n+  final VoidCallback? onBannerRefresh;\r\n+  final Map<String, dynamic>? additionalFormData;\r\n+  final Map<String, dynamic> Function(T? item)? dynamicAdditionalFormData;\r\n+\r\n+  const DynamicGridScreen({\r\n+    super.key,\r\n+    required this.telaNome,\r\n+    required this.fromJson,\r\n+    required this.toJson,\r\n+    required this.hasPermission,\r\n+    this.storageKey,\r\n+    this.onItemTap,\r\n+    this.detailScreenBuilder,\r\n+    this.extraParams,\r\n+    this.onUserBannerTapped,\r\n+    this.onBannerRefresh,\r\n+    this.additionalFormData,\r\n+    this.dynamicAdditionalFormData,\r\n+  });\r\n+\r\n+  @override\r\n+  State<DynamicGridScreen<T>> createState() => _DynamicGridScreenState<T>();\r\n+}\r\n+\r\n+class _DynamicGridScreenState<T> extends State<DynamicGridScreen<T>> {\r\n+  late Future<TelaConfig> _telaFuture;\r\n+  final TelaService _telaService = TelaService(\r\n+    networkCaller: NetworkCaller(),\r\n+    prefs: SharedPreferences.getInstance() as SharedPreferences,\r\n+  );\r\n+\r\n+  @override\r\n+  void initState() {\r\n+    super.initState();\r\n+    _telaFuture = _loadTelaConfig();\r\n+  }\r\n+\r\n+  Future<TelaConfig> _loadTelaConfig() async {\r\n+    // Tenta carregar do cache primeiro\r\n+    final cachedTela = await _telaService.getTelaFromCache(widget.telaNome);\r\n+    if (cachedTela != null) {\r\n+      return cachedTela;\r\n+    }\r\n+\r\n+    // Se não encontrou no cache, busca da API\r\n+    final tela = await _telaService.getTelaByNome(widget.telaNome);\r\n+    if (tela != null) {\r\n+      await _telaService.saveTelaToCache(widget.telaNome, tela);\r\n+      return tela;\r\n+    }\r\n+\r\n+    throw Exception('Tela ${widget.telaNome} não encontrada');\r\n+  }\r\n+\r\n+  List<FieldConfig> _convertToFieldConfigs(List<TelaField> fields,\r\n+      {bool forInsert = true, bool forUpdate = false}) {\r\n+    return fields.where((field) {\r\n+      if (forInsert && !field.showInInsert) return false;\r\n+      if (forUpdate && !field.showInUpdate) return false;\r\n+      return field.isInForm;\r\n+    }).map((field) {\r\n+      return FieldConfig(\r\n+        label: field.label,\r\n+        fieldName: field.fieldName,\r\n+        displayFieldName: field.displayFieldName,\r\n+        isFilterable: field.isFilterable,\r\n+        isInForm: field.isInForm,\r\n+        flex: field.flex,\r\n+        maxLines: field.maxLines,\r\n+        icon: field.iconData,\r\n+        isSortable: field.isSortable,\r\n+        fieldType: field.fieldType,\r\n+        dropdownOptions: field.dropdownOptionsMap,\r\n+        dropdownFutureBuilder: field.dropdownEndpoint != null\r\n+            ? _createDropdownFutureBuilder(field.dropdownEndpoint!)\r\n+            : null,\r\n+        dropdownValueField: field.dropdownValueField,\r\n+        dropdownDisplayField: field.dropdownDisplayField,\r\n+        isRequired: field.isRequired,\r\n+        validator: _createValidator(field),\r\n+        isVisibleByDefault: field.isVisibleByDefault,\r\n+        isFixed: field.isFixed,\r\n+        enabled: field.enabled,\r\n+        defaultValue: field.defaultValue,\r\n+        fileConfig: field.fieldType == FieldType.file ? field.fileConfig : null,\r\n+        dropdownSelectedValue: field.dropdownSelectedValue,\r\n+        showInCard: field.showInCard,\r\n+        firstDate: field.firstDate,\r\n+        lastDate: field.lastDate,\r\n+        dateFormat: field.dateFormat,\r\n+      );\r\n+    }).toList();\r\n+  }\r\n+\r\n+  Future<List<Map<String, dynamic>>> Function()? _createDropdownFutureBuilder(\r\n+      String endpoint) {\r\n+    return () async {\r\n+      try {\r\n+        final response = await NetworkCaller().getRequest(endpoint);\r\n+        if (response.isSuccess && response.body != null) {\r\n+          final data = response.body!['data'] ?? response.body!;\r\n+          return (data as List).cast<Map<String, dynamic>>();\r\n+        }\r\n+        return [];\r\n+      } catch (e) {\r\n+        print('Erro ao carregar dropdown: $e');\r\n+        return [];\r\n+      }\r\n+    };\r\n+  }\r\n+\r\n+  String? Function(String?)? _createValidator(TelaField field) {\r\n+    if (!field.isRequired) return null;\r\n+\r\n+    return (value) {\r\n+      if (value == null || value.isEmpty) {\r\n+        return '${field.label} é obrigatório';\r\n+      }\r\n+\r\n+      // Validações específicas por tipo\r\n+      switch (field.fieldType) {\r\n+        case FieldType.email:\r\n+          if (!RegExp(r'^[\\w-\\.]+@([\\w-]+\\.)+[\\w-]{2,4}$').hasMatch(value)) {\r\n+            return 'Email inválido';\r\n+          }\r\n+          break;\r\n+        case FieldType.cpf:\r\n+          if (!_validateCPF(value)) {\r\n+            return 'CPF inválido';\r\n+          }\r\n+          break;\r\n+        case FieldType.cnpj:\r\n+          if (!_validateCNPJ(value)) {\r\n+            return 'CNPJ inválido';\r\n+          }\r\n+          break;\r\n+        case FieldType.phone:\r\n+          if (!RegExp(r'^\\(\\d{2}\\)\\s\\d{4,5}-\\d{4}$').hasMatch(value)) {\r\n+            return 'Telefone inválido';\r\n+          }\r\n+          break;\r\n+        default:\r\n+          break;\r\n+      }\r\n+\r\n+      return null;\r\n+    };\r\n+  }\r\n+\r\n+  bool _validateCPF(String cpf) {\r\n+    // Implementação da validação de CPF\r\n+    return true;\r\n+  }\r\n+\r\n+  bool _validateCNPJ(String cnpj) {\r\n+    // Implementação da validação de CNPJ\r\n+    return true;\r\n+  }\r\n+\r\n+  @override\r\n+  Widget build(BuildContext context) {\r\n+    return FutureBuilder<TelaConfig>(\r\n+      future: _telaFuture,\r\n+      builder: (context, snapshot) {\r\n+        if (snapshot.connectionState == ConnectionState.waiting) {\r\n+          return const Scaffold(\r\n+            body: Center(child: CircularProgressIndicator()),\r\n+          );\r\n+        }\r\n+\r\n+        if (snapshot.hasError) {\r\n+          return Scaffold(\r\n+            appBar: AppBar(title: const Text('Erro')),\r\n+            body: Center(\r\n+              child: Column(\r\n+                mainAxisAlignment: MainAxisAlignment.center,\r\n+                children: [\r\n+                  const Icon(Icons.error_outline, size: 64, color: Colors.red),\r\n+                  const SizedBox(height: 16),\r\n+                  Text(\r\n+                    'Erro ao carregar tela: ${snapshot.error}',\r\n+                    textAlign: TextAlign.center,\r\n+                    style: const TextStyle(fontSize: 16),\r\n+                  ),\r\n+                  const SizedBox(height: 16),\r\n+                  ElevatedButton(\r\n+                    onPressed: () => setState(() {\r\n+                      _telaFuture = _loadTelaConfig();\r\n+                    }),\r\n+                    child: const Text('Tentar Novamente'),\r\n+                  ),\r\n+                ],\r\n+              ),\r\n+            ),\r\n+          );\r\n+        }\r\n+\r\n+        if (!snapshot.hasData) {\r\n+          return const Scaffold(\r\n+            body: Center(child: Text('Tela não encontrada')),\r\n+          );\r\n+        }\r\n+\r\n+        final telaConfig = snapshot.data!;\r\n+\r\n+        return GenericMobileGridScreen<T>(\r\n+          title: telaConfig.titulo,\r\n+          fetchEndpoint: telaConfig.fetchEndpoint,\r\n+          createEndpoint: telaConfig.createEndpoint,\r\n+          updateEndpoint: telaConfig.updateEndpoint,\r\n+          deleteEndpoint: telaConfig.deleteEndpoint,\r\n+          fromJson: widget.fromJson,\r\n+          toJson: widget.toJson,\r\n+          hasPermission: widget.hasPermission,\r\n+          fieldConfigs: _convertToFieldConfigs(telaConfig.fields),\r\n+          idFieldName: telaConfig.idFieldName,\r\n+          dateFieldName: telaConfig.dateFieldName,\r\n+          storageKey: widget.storageKey ??\r\n+              telaConfig.storageKey ??\r\n+              'dynamic_grid_${telaConfig.nome}',\r\n+          onItemTap: widget.onItemTap,\r\n+          detailScreenBuilder: widget.detailScreenBuilder,\r\n+          extraParams: widget.extraParams,\r\n+          enableSearch: telaConfig.enableSearch,\r\n+          enableDebugMode: telaConfig.enableDebugMode,\r\n+          useUserBannerAppBar: telaConfig.useUserBannerAppBar,\r\n+          onUserBannerTapped: widget.onUserBannerTapped,\r\n+          onBannerRefresh: widget.onBannerRefresh,\r\n+          additionalFormData: widget.additionalFormData,\r\n+          dynamicAdditionalFormData: widget.dynamicAdditionalFormData,\r\n+        );\r\n+      },\r\n+    );\r\n+  }\r\n+}\r\n"
                },
                {
                    "date": 1759781296215,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,261 @@\n+import 'package:file_picker/file_picker.dart';\r\n+import 'package:flutter/material.dart';\r\n+import 'package:intl/intl.dart';\r\n+import 'package:shared_preferences/shared_preferences.dart';\r\n+import 'package:task_manager_flutter/data/models/network_response.dart';\r\n+import 'package:task_manager_flutter/data/services/network_caller.dart';\r\n+import 'package:task_manager_flutter/data/services/upload_file_caller.dart';\r\n+import 'package:task_manager_flutter/data/services/tela_caller.dart';\r\n+import 'package:task_manager_flutter/ui/widgets/user_banners.dart';\r\n+import 'package:task_manager_flutter/data/customization/generic_grid_card.dart';\r\n+import '../models/telas_model.dart';\r\n+// ==============================================\r\n+// MOBILE GRID SCREEN - MATERIAL DESIGN 3 COMPLETO\r\n+// ==============================================\r\n+\r\n+// screens/dynamic_grid_screen.dart\r\n+class DynamicGridScreen<T> extends StatefulWidget {\r\n+  final String telaNome;\r\n+  final FromJson<T> fromJson;\r\n+  final ToJson<T> toJson;\r\n+  final SecurityCheck hasPermission;\r\n+  final String? storageKey;\r\n+  final OnItemTap<T>? onItemTap;\r\n+  final Widget Function(T item)? detailScreenBuilder;\r\n+  final Map<String, dynamic>? extraParams;\r\n+  final VoidCallback? onUserBannerTapped;\r\n+  final VoidCallback? onBannerRefresh;\r\n+  final Map<String, dynamic>? additionalFormData;\r\n+  final Map<String, dynamic> Function(T? item)? dynamicAdditionalFormData;\r\n+\r\n+  const DynamicGridScreen({\r\n+    super.key,\r\n+    required this.telaNome,\r\n+    required this.fromJson,\r\n+    required this.toJson,\r\n+    required this.hasPermission,\r\n+    this.storageKey,\r\n+    this.onItemTap,\r\n+    this.detailScreenBuilder,\r\n+    this.extraParams,\r\n+    this.onUserBannerTapped,\r\n+    this.onBannerRefresh,\r\n+    this.additionalFormData,\r\n+    this.dynamicAdditionalFormData,\r\n+  });\r\n+\r\n+  @override\r\n+  State<DynamicGridScreen<T>> createState() => _DynamicGridScreenState<T>();\r\n+}\r\n+\r\n+class _DynamicGridScreenState<T> extends State<DynamicGridScreen<T>> {\r\n+  late Future<TelaConfig> _telaFuture;\r\n+  final TelaService _telaService = TelaService(\r\n+    networkCaller: NetworkCaller(),\r\n+    prefs: SharedPreferences.getInstance() as SharedPreferences,\r\n+  );\r\n+\r\n+  @override\r\n+  void initState() {\r\n+    super.initState();\r\n+    _telaFuture = _loadTelaConfig();\r\n+  }\r\n+\r\n+  Future<TelaConfig> _loadTelaConfig() async {\r\n+    // Tenta carregar do cache primeiro\r\n+    final cachedTela = await _telaService.getTelaFromCache(widget.telaNome);\r\n+    if (cachedTela != null) {\r\n+      return cachedTela;\r\n+    }\r\n+\r\n+    // Se não encontrou no cache, busca da API\r\n+    final tela = await _telaService.getTelaByNome(widget.telaNome);\r\n+    if (tela != null) {\r\n+      await _telaService.saveTelaToCache(widget.telaNome, tela);\r\n+      return tela;\r\n+    }\r\n+\r\n+    throw Exception('Tela ${widget.telaNome} não encontrada');\r\n+  }\r\n+\r\n+  List<FieldConfig> _convertToFieldConfigs(List<TelaField> fields,\r\n+      {bool forInsert = true, bool forUpdate = false}) {\r\n+    return fields.where((field) {\r\n+      if (forInsert && !field.showInInsert) return false;\r\n+      if (forUpdate && !field.showInUpdate) return false;\r\n+      return field.isInForm;\r\n+    }).map((field) {\r\n+      return FieldConfig(\r\n+        label: field.label,\r\n+        fieldName: field.fieldName,\r\n+        displayFieldName: field.displayFieldName,\r\n+        isFilterable: field.isFilterable,\r\n+        isInForm: field.isInForm,\r\n+        flex: field.flex,\r\n+        maxLines: field.maxLines,\r\n+        icon: field.iconData,\r\n+        isSortable: field.isSortable,\r\n+        fieldType: field.fieldType,\r\n+        dropdownOptions: field.dropdownOptionsMap,\r\n+        dropdownFutureBuilder: field.dropdownEndpoint != null\r\n+            ? _createDropdownFutureBuilder(field.dropdownEndpoint!)\r\n+            : null,\r\n+        dropdownValueField: field.dropdownValueField,\r\n+        dropdownDisplayField: field.dropdownDisplayField,\r\n+        isRequired: field.isRequired,\r\n+        validator: _createValidator(field),\r\n+        isVisibleByDefault: field.isVisibleByDefault,\r\n+        isFixed: field.isFixed,\r\n+        enabled: field.enabled,\r\n+        defaultValue: field.defaultValue,\r\n+        fileConfig: field.fieldType == FieldType.file ? field.fileConfig : null,\r\n+        dropdownSelectedValue: field.dropdownSelectedValue,\r\n+        showInCard: field.showInCard,\r\n+        firstDate: field.firstDate,\r\n+        lastDate: field.lastDate,\r\n+        dateFormat: field.dateFormat,\r\n+      );\r\n+    }).toList();\r\n+  }\r\n+\r\n+  Future<List<Map<String, dynamic>>> Function()? _createDropdownFutureBuilder(\r\n+      String endpoint) {\r\n+    return () async {\r\n+      try {\r\n+        final response = await NetworkCaller().getRequest(endpoint);\r\n+        if (response.isSuccess && response.body != null) {\r\n+          final data = response.body!['data'] ?? response.body!;\r\n+          return (data as List).cast<Map<String, dynamic>>();\r\n+        }\r\n+        return [];\r\n+      } catch (e) {\r\n+        print('Erro ao carregar dropdown: $e');\r\n+        return [];\r\n+      }\r\n+    };\r\n+  }\r\n+\r\n+  String? Function(String?)? _createValidator(TelaField field) {\r\n+    if (!field.isRequired) return null;\r\n+\r\n+    return (value) {\r\n+      if (value == null || value.isEmpty) {\r\n+        return '${field.label} é obrigatório';\r\n+      }\r\n+\r\n+      // Validações específicas por tipo\r\n+      switch (field.fieldType) {\r\n+        case FieldType.email:\r\n+          if (!RegExp(r'^[\\w-\\.]+@([\\w-]+\\.)+[\\w-]{2,4}$').hasMatch(value)) {\r\n+            return 'Email inválido';\r\n+          }\r\n+          break;\r\n+        case FieldType.cpf:\r\n+          if (!_validateCPF(value)) {\r\n+            return 'CPF inválido';\r\n+          }\r\n+          break;\r\n+        case FieldType.cnpj:\r\n+          if (!_validateCNPJ(value)) {\r\n+            return 'CNPJ inválido';\r\n+          }\r\n+          break;\r\n+        case FieldType.phone:\r\n+          if (!RegExp(r'^\\(\\d{2}\\)\\s\\d{4,5}-\\d{4}$').hasMatch(value)) {\r\n+            return 'Telefone inválido';\r\n+          }\r\n+          break;\r\n+        default:\r\n+          break;\r\n+      }\r\n+\r\n+      return null;\r\n+    };\r\n+  }\r\n+\r\n+  bool _validateCPF(String cpf) {\r\n+    // Implementação da validação de CPF\r\n+    return true;\r\n+  }\r\n+\r\n+  bool _validateCNPJ(String cnpj) {\r\n+    // Implementação da validação de CNPJ\r\n+    return true;\r\n+  }\r\n+\r\n+  @override\r\n+  Widget build(BuildContext context) {\r\n+    return FutureBuilder<TelaConfig>(\r\n+      future: _telaFuture,\r\n+      builder: (context, snapshot) {\r\n+        if (snapshot.connectionState == ConnectionState.waiting) {\r\n+          return const Scaffold(\r\n+            body: Center(child: CircularProgressIndicator()),\r\n+          );\r\n+        }\r\n+\r\n+        if (snapshot.hasError) {\r\n+          return Scaffold(\r\n+            appBar: AppBar(title: const Text('Erro')),\r\n+            body: Center(\r\n+              child: Column(\r\n+                mainAxisAlignment: MainAxisAlignment.center,\r\n+                children: [\r\n+                  const Icon(Icons.error_outline, size: 64, color: Colors.red),\r\n+                  const SizedBox(height: 16),\r\n+                  Text(\r\n+                    'Erro ao carregar tela: ${snapshot.error}',\r\n+                    textAlign: TextAlign.center,\r\n+                    style: const TextStyle(fontSize: 16),\r\n+                  ),\r\n+                  const SizedBox(height: 16),\r\n+                  ElevatedButton(\r\n+                    onPressed: () => setState(() {\r\n+                      _telaFuture = _loadTelaConfig();\r\n+                    }),\r\n+                    child: const Text('Tentar Novamente'),\r\n+                  ),\r\n+                ],\r\n+              ),\r\n+            ),\r\n+          );\r\n+        }\r\n+\r\n+        if (!snapshot.hasData) {\r\n+          return const Scaffold(\r\n+            body: Center(child: Text('Tela não encontrada')),\r\n+          );\r\n+        }\r\n+\r\n+        final telaConfig = snapshot.data!;\r\n+\r\n+        return GenericMobileGridScreen<T>(\r\n+          title: telaConfig.titulo,\r\n+          fetchEndpoint: telaConfig.fetchEndpoint,\r\n+          createEndpoint: telaConfig.createEndpoint,\r\n+          updateEndpoint: telaConfig.updateEndpoint,\r\n+          deleteEndpoint: telaConfig.deleteEndpoint,\r\n+          fromJson: widget.fromJson,\r\n+          toJson: widget.toJson,\r\n+          hasPermission: widget.hasPermission,\r\n+          fieldConfigs: _convertToFieldConfigs(telaConfig.fields),\r\n+          idFieldName: telaConfig.idFieldName,\r\n+          dateFieldName: telaConfig.dateFieldName,\r\n+          storageKey: widget.storageKey ??\r\n+              telaConfig.storageKey ??\r\n+              'dynamic_grid_${telaConfig.nome}',\r\n+          onItemTap: widget.onItemTap,\r\n+          detailScreenBuilder: widget.detailScreenBuilder,\r\n+          extraParams: widget.extraParams,\r\n+          enableSearch: telaConfig.enableSearch,\r\n+          enableDebugMode: telaConfig.enableDebugMode,\r\n+          useUserBannerAppBar: telaConfig.useUserBannerAppBar,\r\n+          onUserBannerTapped: widget.onUserBannerTapped,\r\n+          onBannerRefresh: widget.onBannerRefresh,\r\n+          additionalFormData: widget.additionalFormData,\r\n+          dynamicAdditionalFormData: widget.dynamicAdditionalFormData,\r\n+        );\r\n+      },\r\n+    );\r\n+  }\r\n+}\r\n"
                },
                {
                    "date": 1759781350607,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,257 @@\n+import 'package:flutter/material.dart';\r\n+import 'package:shared_preferences/shared_preferences.dart';\r\n+import 'package:task_manager_flutter/data/customization/generic_grid_card.dart';\r\n+import 'package:task_manager_flutter/data/services/network_caller.dart';\r\n+import 'package:task_manager_flutter/data/services/tela_caller.dart';\r\n+\r\n+import '../models/telas_model.dart';\r\n+// ==============================================\r\n+// MOBILE GRID SCREEN - MATERIAL DESIGN 3 COMPLETO\r\n+// ==============================================\r\n+\r\n+// screens/dynamic_grid_screen.dart\r\n+class DynamicGridScreen<T> extends StatefulWidget {\r\n+  final String telaNome;\r\n+  final FromJson<T> fromJson;\r\n+  final ToJson<T> toJson;\r\n+  final SecurityCheck hasPermission;\r\n+  final String? storageKey;\r\n+  final OnItemTap<T>? onItemTap;\r\n+  final Widget Function(T item)? detailScreenBuilder;\r\n+  final Map<String, dynamic>? extraParams;\r\n+  final VoidCallback? onUserBannerTapped;\r\n+  final VoidCallback? onBannerRefresh;\r\n+  final Map<String, dynamic>? additionalFormData;\r\n+  final Map<String, dynamic> Function(T? item)? dynamicAdditionalFormData;\r\n+\r\n+  const DynamicGridScreen({\r\n+    super.key,\r\n+    required this.telaNome,\r\n+    required this.fromJson,\r\n+    required this.toJson,\r\n+    required this.hasPermission,\r\n+    this.storageKey,\r\n+    this.onItemTap,\r\n+    this.detailScreenBuilder,\r\n+    this.extraParams,\r\n+    this.onUserBannerTapped,\r\n+    this.onBannerRefresh,\r\n+    this.additionalFormData,\r\n+    this.dynamicAdditionalFormData,\r\n+  });\r\n+\r\n+  @override\r\n+  State<DynamicGridScreen<T>> createState() => _DynamicGridScreenState<T>();\r\n+}\r\n+\r\n+class _DynamicGridScreenState<T> extends State<DynamicGridScreen<T>> {\r\n+  late Future<TelaConfig> _telaFuture;\r\n+  final TelaService _telaService = TelaService(\r\n+    networkCaller: NetworkCaller(),\r\n+    prefs: SharedPreferences.getInstance() as SharedPreferences,\r\n+  );\r\n+\r\n+  @override\r\n+  void initState() {\r\n+    super.initState();\r\n+    _telaFuture = _loadTelaConfig();\r\n+  }\r\n+\r\n+  Future<TelaConfig> _loadTelaConfig() async {\r\n+    // Tenta carregar do cache primeiro\r\n+    final cachedTela = await _telaService.getTelaFromCache(widget.telaNome);\r\n+    if (cachedTela != null) {\r\n+      return cachedTela;\r\n+    }\r\n+\r\n+    // Se não encontrou no cache, busca da API\r\n+    final tela = await _telaService.getTelaByNome(widget.telaNome);\r\n+    if (tela != null) {\r\n+      await _telaService.saveTelaToCache(widget.telaNome, tela);\r\n+      return tela;\r\n+    }\r\n+\r\n+    throw Exception('Tela ${widget.telaNome} não encontrada');\r\n+  }\r\n+\r\n+  List<FieldConfig> _convertToFieldConfigs(List<TelaField> fields,\r\n+      {bool forInsert = true, bool forUpdate = false}) {\r\n+    return fields.where((field) {\r\n+      if (forInsert && !field.showInInsert) return false;\r\n+      if (forUpdate && !field.showInUpdate) return false;\r\n+      return field.isInForm;\r\n+    }).map((field) {\r\n+      return FieldConfig(\r\n+        label: field.label,\r\n+        fieldName: field.fieldName,\r\n+        displayFieldName: field.displayFieldName,\r\n+        isFilterable: field.isFilterable,\r\n+        isInForm: field.isInForm,\r\n+        flex: field.flex,\r\n+        maxLines: field.maxLines,\r\n+        icon: field.iconData,\r\n+        isSortable: field.isSortable,\r\n+        fieldType: field.fieldType,\r\n+        dropdownOptions: field.dropdownOptionsMap,\r\n+        dropdownFutureBuilder: field.dropdownEndpoint != null\r\n+            ? _createDropdownFutureBuilder(field.dropdownEndpoint!)\r\n+            : null,\r\n+        dropdownValueField: field.dropdownValueField,\r\n+        dropdownDisplayField: field.dropdownDisplayField,\r\n+        isRequired: field.isRequired,\r\n+        validator: _createValidator(field),\r\n+        isVisibleByDefault: field.isVisibleByDefault,\r\n+        isFixed: field.isFixed,\r\n+        enabled: field.enabled,\r\n+        defaultValue: field.defaultValue,\r\n+        fileConfig: field.fieldType == FieldType.file ? field.fileConfig : null,\r\n+        dropdownSelectedValue: field.dropdownSelectedValue,\r\n+        showInCard: field.showInCard,\r\n+        firstDate: field.firstDate,\r\n+        lastDate: field.lastDate,\r\n+        dateFormat: field.dateFormat,\r\n+      );\r\n+    }).toList();\r\n+  }\r\n+\r\n+  Future<List<Map<String, dynamic>>> Function()? _createDropdownFutureBuilder(\r\n+      String endpoint) {\r\n+    return () async {\r\n+      try {\r\n+        final response = await NetworkCaller().getRequest(endpoint);\r\n+        if (response.isSuccess && response.body != null) {\r\n+          final data = response.body!['data'] ?? response.body!;\r\n+          return (data as List).cast<Map<String, dynamic>>();\r\n+        }\r\n+        return [];\r\n+      } catch (e) {\r\n+        print('Erro ao carregar dropdown: $e');\r\n+        return [];\r\n+      }\r\n+    };\r\n+  }\r\n+\r\n+  String? Function(String?)? _createValidator(TelaField field) {\r\n+    if (!field.isRequired) return null;\r\n+\r\n+    return (value) {\r\n+      if (value == null || value.isEmpty) {\r\n+        return '${field.label} é obrigatório';\r\n+      }\r\n+\r\n+      // Validações específicas por tipo\r\n+      switch (field.fieldType) {\r\n+        case FieldType.email:\r\n+          if (!RegExp(r'^[\\w-\\.]+@([\\w-]+\\.)+[\\w-]{2,4}$').hasMatch(value)) {\r\n+            return 'Email inválido';\r\n+          }\r\n+          break;\r\n+        case FieldType.cpf:\r\n+          if (!_validateCPF(value)) {\r\n+            return 'CPF inválido';\r\n+          }\r\n+          break;\r\n+        case FieldType.cnpj:\r\n+          if (!_validateCNPJ(value)) {\r\n+            return 'CNPJ inválido';\r\n+          }\r\n+          break;\r\n+        case FieldType.phone:\r\n+          if (!RegExp(r'^\\(\\d{2}\\)\\s\\d{4,5}-\\d{4}$').hasMatch(value)) {\r\n+            return 'Telefone inválido';\r\n+          }\r\n+          break;\r\n+        default:\r\n+          break;\r\n+      }\r\n+\r\n+      return null;\r\n+    };\r\n+  }\r\n+\r\n+  bool _validateCPF(String cpf) {\r\n+    // Implementação da validação de CPF\r\n+    return true;\r\n+  }\r\n+\r\n+  bool _validateCNPJ(String cnpj) {\r\n+    // Implementação da validação de CNPJ\r\n+    return true;\r\n+  }\r\n+\r\n+  @override\r\n+  Widget build(BuildContext context) {\r\n+    return FutureBuilder<TelaConfig>(\r\n+      future: _telaFuture,\r\n+      builder: (context, snapshot) {\r\n+        if (snapshot.connectionState == ConnectionState.waiting) {\r\n+          return const Scaffold(\r\n+            body: Center(child: CircularProgressIndicator()),\r\n+          );\r\n+        }\r\n+\r\n+        if (snapshot.hasError) {\r\n+          return Scaffold(\r\n+            appBar: AppBar(title: const Text('Erro')),\r\n+            body: Center(\r\n+              child: Column(\r\n+                mainAxisAlignment: MainAxisAlignment.center,\r\n+                children: [\r\n+                  const Icon(Icons.error_outline, size: 64, color: Colors.red),\r\n+                  const SizedBox(height: 16),\r\n+                  Text(\r\n+                    'Erro ao carregar tela: ${snapshot.error}',\r\n+                    textAlign: TextAlign.center,\r\n+                    style: const TextStyle(fontSize: 16),\r\n+                  ),\r\n+                  const SizedBox(height: 16),\r\n+                  ElevatedButton(\r\n+                    onPressed: () => setState(() {\r\n+                      _telaFuture = _loadTelaConfig();\r\n+                    }),\r\n+                    child: const Text('Tentar Novamente'),\r\n+                  ),\r\n+                ],\r\n+              ),\r\n+            ),\r\n+          );\r\n+        }\r\n+\r\n+        if (!snapshot.hasData) {\r\n+          return const Scaffold(\r\n+            body: Center(child: Text('Tela não encontrada')),\r\n+          );\r\n+        }\r\n+\r\n+        final telaConfig = snapshot.data!;\r\n+\r\n+        return GenericMobileGridScreen<T>(\r\n+          title: telaConfig.titulo,\r\n+          fetchEndpoint: telaConfig.fetchEndpoint,\r\n+          createEndpoint: telaConfig.createEndpoint,\r\n+          updateEndpoint: telaConfig.updateEndpoint,\r\n+          deleteEndpoint: telaConfig.deleteEndpoint,\r\n+          fromJson: widget.fromJson,\r\n+          toJson: widget.toJson,\r\n+          hasPermission: widget.hasPermission,\r\n+          fieldConfigs: _convertToFieldConfigs(telaConfig.fields),\r\n+          idFieldName: telaConfig.idFieldName,\r\n+          dateFieldName: telaConfig.dateFieldName,\r\n+          storageKey: widget.storageKey ??\r\n+              telaConfig.storageKey ??\r\n+              'dynamic_grid_${telaConfig.nome}',\r\n+          onItemTap: widget.onItemTap,\r\n+          detailScreenBuilder: widget.detailScreenBuilder,\r\n+          extraParams: widget.extraParams,\r\n+          enableSearch: telaConfig.enableSearch,\r\n+          enableDebugMode: telaConfig.enableDebugMode,\r\n+          useUserBannerAppBar: telaConfig.useUserBannerAppBar,\r\n+          onUserBannerTapped: widget.onUserBannerTapped,\r\n+          onBannerRefresh: widget.onBannerRefresh,\r\n+          additionalFormData: widget.additionalFormData,\r\n+          dynamicAdditionalFormData: widget.dynamicAdditionalFormData,\r\n+        );\r\n+      },\r\n+    );\r\n+  }\r\n+}\r\n"
                },
                {
                    "date": 1759782266964,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -4,13 +4,16 @@\n import 'package:task_manager_flutter/data/services/network_caller.dart';\r\n import 'package:task_manager_flutter/data/services/tela_caller.dart';\r\n \r\n import '../models/telas_model.dart';\r\n-// ==============================================\r\n-// MOBILE GRID SCREEN - MATERIAL DESIGN 3 COMPLETO\r\n-// ==============================================\r\n+// screens/dynamic_grid_screen.dart\r\n+import 'package:flutter/material.dart';\r\n+import 'package:task_manager_flutter/ui/screens/generic_mobile_grid_screen.dart';\r\n+import 'package:task_manager_flutter/data/services/network_caller.dart';\r\n+import '../services/tela_service.dart';\r\n+import '../services/auth_service.dart';\r\n+import '../models/tela_config.dart';\r\n \r\n-// screens/dynamic_grid_screen.dart\r\n class DynamicGridScreen<T> extends StatefulWidget {\r\n   final String telaNome;\r\n   final FromJson<T> fromJson;\r\n   final ToJson<T> toJson;\r\n@@ -45,16 +48,14 @@\n }\r\n \r\n class _DynamicGridScreenState<T> extends State<DynamicGridScreen<T>> {\r\n   late Future<TelaConfig> _telaFuture;\r\n-  final TelaService _telaService = TelaService(\r\n-    networkCaller: NetworkCaller(),\r\n-    prefs: SharedPreferences.getInstance() as SharedPreferences,\r\n-  );\r\n+  late TelaService _telaService;\r\n \r\n   @override\r\n   void initState() {\r\n     super.initState();\r\n+    _telaService = TelaService(networkCaller: NetworkCaller());\r\n     _telaFuture = _loadTelaConfig();\r\n   }\r\n \r\n   Future<TelaConfig> _loadTelaConfig() async {\r\n@@ -73,10 +74,9 @@\n \r\n     throw Exception('Tela ${widget.telaNome} não encontrada');\r\n   }\r\n \r\n-  List<FieldConfig> _convertToFieldConfigs(List<TelaField> fields,\r\n-      {bool forInsert = true, bool forUpdate = false}) {\r\n+  List<FieldConfig> _convertToFieldConfigs(List<TelaField> fields, {bool forInsert = true, bool forUpdate = false}) {\r\n     return fields.where((field) {\r\n       if (forInsert && !field.showInInsert) return false;\r\n       if (forUpdate && !field.showInUpdate) return false;\r\n       return field.isInForm;\r\n@@ -91,10 +91,15 @@\n         maxLines: field.maxLines,\r\n         icon: field.iconData,\r\n         isSortable: field.isSortable,\r\n         fieldType: field.fieldType,\r\n-        dropdownOptions: field.dropdownOptionsMap,\r\n-        dropdownFutureBuilder: field.dropdownEndpoint != null\r\n+        dropdownOptions: field.dropdownOptions.isNotEmpty \r\n+            ? field.dropdownOptions.map((option) => {\r\n+                'value': option.optionValue,\r\n+                'label': option.optionLabel,\r\n+              }).toList()\r\n+            : null,\r\n+        dropdownFutureBuilder: field.dropdownEndpoint != null \r\n             ? _createDropdownFutureBuilder(field.dropdownEndpoint!)\r\n             : null,\r\n         dropdownValueField: field.dropdownValueField,\r\n         dropdownDisplayField: field.dropdownDisplayField,\r\n@@ -103,9 +108,16 @@\n         isVisibleByDefault: field.isVisibleByDefault,\r\n         isFixed: field.isFixed,\r\n         enabled: field.enabled,\r\n         defaultValue: field.defaultValue,\r\n-        fileConfig: field.fieldType == FieldType.file ? field.fileConfig : null,\r\n+        fileConfig: field.fieldType == FieldType.file \r\n+            ? FileConfig(\r\n+                allowedExtensions: field.allowedExtensions,\r\n+                allowMultiple: field.allowMultipleFiles,\r\n+                maxFileSize: field.maxFileSize,\r\n+                fileFieldName: field.fileFieldName,\r\n+              )\r\n+            : null,\r\n         dropdownSelectedValue: field.dropdownSelectedValue,\r\n         showInCard: field.showInCard,\r\n         firstDate: field.firstDate,\r\n         lastDate: field.lastDate,\r\n@@ -113,16 +125,17 @@\n       );\r\n     }).toList();\r\n   }\r\n \r\n-  Future<List<Map<String, dynamic>>> Function()? _createDropdownFutureBuilder(\r\n-      String endpoint) {\r\n+  Future<List<Map<String, dynamic>>> Function()? _createDropdownFutureBuilder(String endpoint) {\r\n     return () async {\r\n       try {\r\n         final response = await NetworkCaller().getRequest(endpoint);\r\n         if (response.isSuccess && response.body != null) {\r\n           final data = response.body!['data'] ?? response.body!;\r\n-          return (data as List).cast<Map<String, dynamic>>();\r\n+          if (data is List) {\r\n+            return data.cast<Map<String, dynamic>>();\r\n+          }\r\n         }\r\n         return [];\r\n       } catch (e) {\r\n         print('Erro ao carregar dropdown: $e');\r\n@@ -137,9 +150,9 @@\n     return (value) {\r\n       if (value == null || value.isEmpty) {\r\n         return '${field.label} é obrigatório';\r\n       }\r\n-\r\n+      \r\n       // Validações específicas por tipo\r\n       switch (field.fieldType) {\r\n         case FieldType.email:\r\n           if (!RegExp(r'^[\\w-\\.]+@([\\w-]+\\.)+[\\w-]{2,4}$').hasMatch(value)) {\r\n@@ -163,21 +176,23 @@\n           break;\r\n         default:\r\n           break;\r\n       }\r\n-\r\n+      \r\n       return null;\r\n     };\r\n   }\r\n \r\n   bool _validateCPF(String cpf) {\r\n-    // Implementação da validação de CPF\r\n-    return true;\r\n+    // Implementação simplificada - substitua pela validação real\r\n+    final cleaned = cpf.replaceAll(RegExp(r'[^\\d]'), '');\r\n+    return cleaned.length == 11;\r\n   }\r\n \r\n   bool _validateCNPJ(String cnpj) {\r\n-    // Implementação da validação de CNPJ\r\n-    return true;\r\n+    // Implementação simplificada - substitua pela validação real\r\n+    final cleaned = cnpj.replaceAll(RegExp(r'[^\\d]'), '');\r\n+    return cleaned.length == 14;\r\n   }\r\n \r\n   @override\r\n   Widget build(BuildContext context) {\r\n@@ -236,11 +251,9 @@\n           hasPermission: widget.hasPermission,\r\n           fieldConfigs: _convertToFieldConfigs(telaConfig.fields),\r\n           idFieldName: telaConfig.idFieldName,\r\n           dateFieldName: telaConfig.dateFieldName,\r\n-          storageKey: widget.storageKey ??\r\n-              telaConfig.storageKey ??\r\n-              'dynamic_grid_${telaConfig.nome}',\r\n+          storageKey: widget.storageKey ?? telaConfig.storageKey ?? 'dynamic_grid_${telaConfig.nome}',\r\n           onItemTap: widget.onItemTap,\r\n           detailScreenBuilder: widget.detailScreenBuilder,\r\n           extraParams: widget.extraParams,\r\n           enableSearch: telaConfig.enableSearch,\r\n@@ -253,5 +266,5 @@\n         );\r\n       },\r\n     );\r\n   }\r\n-}\r\n+}\n\\ No newline at end of file\n"
                },
                {
                    "date": 1759782282556,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,268 @@\n+import 'package:flutter/material.dart';\r\n+import 'package:task_manager_flutter/data/customization/generic_grid_card.dart';\r\n+import 'package:task_manager_flutter/data/services/network_caller.dart';\r\n+import 'package:task_manager_flutter/data/services/tela_caller.dart';\r\n+\r\n+import '../models/telas_model.dart';\r\n+\r\n+class DynamicGridScreen<T> extends StatefulWidget {\r\n+  final String telaNome;\r\n+  final FromJson<T> fromJson;\r\n+  final ToJson<T> toJson;\r\n+  final SecurityCheck hasPermission;\r\n+  final String? storageKey;\r\n+  final OnItemTap<T>? onItemTap;\r\n+  final Widget Function(T item)? detailScreenBuilder;\r\n+  final Map<String, dynamic>? extraParams;\r\n+  final VoidCallback? onUserBannerTapped;\r\n+  final VoidCallback? onBannerRefresh;\r\n+  final Map<String, dynamic>? additionalFormData;\r\n+  final Map<String, dynamic> Function(T? item)? dynamicAdditionalFormData;\r\n+\r\n+  const DynamicGridScreen({\r\n+    super.key,\r\n+    required this.telaNome,\r\n+    required this.fromJson,\r\n+    required this.toJson,\r\n+    required this.hasPermission,\r\n+    this.storageKey,\r\n+    this.onItemTap,\r\n+    this.detailScreenBuilder,\r\n+    this.extraParams,\r\n+    this.onUserBannerTapped,\r\n+    this.onBannerRefresh,\r\n+    this.additionalFormData,\r\n+    this.dynamicAdditionalFormData,\r\n+  });\r\n+\r\n+  @override\r\n+  State<DynamicGridScreen<T>> createState() => _DynamicGridScreenState<T>();\r\n+}\r\n+\r\n+class _DynamicGridScreenState<T> extends State<DynamicGridScreen<T>> {\r\n+  late Future<TelaConfig> _telaFuture;\r\n+  late TelaService _telaService;\r\n+\r\n+  @override\r\n+  void initState() {\r\n+    super.initState();\r\n+    _telaService = TelaService(networkCaller: NetworkCaller());\r\n+    _telaFuture = _loadTelaConfig();\r\n+  }\r\n+\r\n+  Future<TelaConfig> _loadTelaConfig() async {\r\n+    // Tenta carregar do cache primeiro\r\n+    final cachedTela = await _telaService.getTelaFromCache(widget.telaNome);\r\n+    if (cachedTela != null) {\r\n+      return cachedTela;\r\n+    }\r\n+\r\n+    // Se não encontrou no cache, busca da API\r\n+    final tela = await _telaService.getTelaByNome(widget.telaNome);\r\n+    if (tela != null) {\r\n+      await _telaService.saveTelaToCache(widget.telaNome, tela);\r\n+      return tela;\r\n+    }\r\n+\r\n+    throw Exception('Tela ${widget.telaNome} não encontrada');\r\n+  }\r\n+\r\n+  List<FieldConfig> _convertToFieldConfigs(List<TelaField> fields,\r\n+      {bool forInsert = true, bool forUpdate = false}) {\r\n+    return fields.where((field) {\r\n+      if (forInsert && !field.showInInsert) return false;\r\n+      if (forUpdate && !field.showInUpdate) return false;\r\n+      return field.isInForm;\r\n+    }).map((field) {\r\n+      return FieldConfig(\r\n+        label: field.label,\r\n+        fieldName: field.fieldName,\r\n+        displayFieldName: field.displayFieldName,\r\n+        isFilterable: field.isFilterable,\r\n+        isInForm: field.isInForm,\r\n+        flex: field.flex,\r\n+        maxLines: field.maxLines,\r\n+        icon: field.iconData,\r\n+        isSortable: field.isSortable,\r\n+        fieldType: field.fieldType,\r\n+        dropdownOptions: field.dropdownOptions.isNotEmpty\r\n+            ? field.dropdownOptions\r\n+                .map((option) => {\r\n+                      'value': option.optionValue,\r\n+                      'label': option.optionLabel,\r\n+                    })\r\n+                .toList()\r\n+            : null,\r\n+        dropdownFutureBuilder: field.dropdownEndpoint != null\r\n+            ? _createDropdownFutureBuilder(field.dropdownEndpoint!)\r\n+            : null,\r\n+        dropdownValueField: field.dropdownValueField,\r\n+        dropdownDisplayField: field.dropdownDisplayField,\r\n+        isRequired: field.isRequired,\r\n+        validator: _createValidator(field),\r\n+        isVisibleByDefault: field.isVisibleByDefault,\r\n+        isFixed: field.isFixed,\r\n+        enabled: field.enabled,\r\n+        defaultValue: field.defaultValue,\r\n+        fileConfig: field.fieldType == FieldType.file\r\n+            ? FileConfig(\r\n+                allowedExtensions: field.allowedExtensions,\r\n+                allowMultiple: field.allowMultipleFiles,\r\n+                maxFileSize: field.maxFileSize,\r\n+                fileFieldName: field.fileFieldName,\r\n+              )\r\n+            : null,\r\n+        dropdownSelectedValue: field.dropdownSelectedValue,\r\n+        showInCard: field.showInCard,\r\n+        firstDate: field.firstDate,\r\n+        lastDate: field.lastDate,\r\n+        dateFormat: field.dateFormat,\r\n+      );\r\n+    }).toList();\r\n+  }\r\n+\r\n+  Future<List<Map<String, dynamic>>> Function()? _createDropdownFutureBuilder(\r\n+      String endpoint) {\r\n+    return () async {\r\n+      try {\r\n+        final response = await NetworkCaller().getRequest(endpoint);\r\n+        if (response.isSuccess && response.body != null) {\r\n+          final data = response.body!['data'] ?? response.body!;\r\n+          if (data is List) {\r\n+            return data.cast<Map<String, dynamic>>();\r\n+          }\r\n+        }\r\n+        return [];\r\n+      } catch (e) {\r\n+        print('Erro ao carregar dropdown: $e');\r\n+        return [];\r\n+      }\r\n+    };\r\n+  }\r\n+\r\n+  String? Function(String?)? _createValidator(TelaField field) {\r\n+    if (!field.isRequired) return null;\r\n+\r\n+    return (value) {\r\n+      if (value == null || value.isEmpty) {\r\n+        return '${field.label} é obrigatório';\r\n+      }\r\n+\r\n+      // Validações específicas por tipo\r\n+      switch (field.fieldType) {\r\n+        case FieldType.email:\r\n+          if (!RegExp(r'^[\\w-\\.]+@([\\w-]+\\.)+[\\w-]{2,4}$').hasMatch(value)) {\r\n+            return 'Email inválido';\r\n+          }\r\n+          break;\r\n+        case FieldType.cpf:\r\n+          if (!_validateCPF(value)) {\r\n+            return 'CPF inválido';\r\n+          }\r\n+          break;\r\n+        case FieldType.cnpj:\r\n+          if (!_validateCNPJ(value)) {\r\n+            return 'CNPJ inválido';\r\n+          }\r\n+          break;\r\n+        case FieldType.phone:\r\n+          if (!RegExp(r'^\\(\\d{2}\\)\\s\\d{4,5}-\\d{4}$').hasMatch(value)) {\r\n+            return 'Telefone inválido';\r\n+          }\r\n+          break;\r\n+        default:\r\n+          break;\r\n+      }\r\n+\r\n+      return null;\r\n+    };\r\n+  }\r\n+\r\n+  bool _validateCPF(String cpf) {\r\n+    // Implementação simplificada - substitua pela validação real\r\n+    final cleaned = cpf.replaceAll(RegExp(r'[^\\d]'), '');\r\n+    return cleaned.length == 11;\r\n+  }\r\n+\r\n+  bool _validateCNPJ(String cnpj) {\r\n+    // Implementação simplificada - substitua pela validação real\r\n+    final cleaned = cnpj.replaceAll(RegExp(r'[^\\d]'), '');\r\n+    return cleaned.length == 14;\r\n+  }\r\n+\r\n+  @override\r\n+  Widget build(BuildContext context) {\r\n+    return FutureBuilder<TelaConfig>(\r\n+      future: _telaFuture,\r\n+      builder: (context, snapshot) {\r\n+        if (snapshot.connectionState == ConnectionState.waiting) {\r\n+          return const Scaffold(\r\n+            body: Center(child: CircularProgressIndicator()),\r\n+          );\r\n+        }\r\n+\r\n+        if (snapshot.hasError) {\r\n+          return Scaffold(\r\n+            appBar: AppBar(title: const Text('Erro')),\r\n+            body: Center(\r\n+              child: Column(\r\n+                mainAxisAlignment: MainAxisAlignment.center,\r\n+                children: [\r\n+                  const Icon(Icons.error_outline, size: 64, color: Colors.red),\r\n+                  const SizedBox(height: 16),\r\n+                  Text(\r\n+                    'Erro ao carregar tela: ${snapshot.error}',\r\n+                    textAlign: TextAlign.center,\r\n+                    style: const TextStyle(fontSize: 16),\r\n+                  ),\r\n+                  const SizedBox(height: 16),\r\n+                  ElevatedButton(\r\n+                    onPressed: () => setState(() {\r\n+                      _telaFuture = _loadTelaConfig();\r\n+                    }),\r\n+                    child: const Text('Tentar Novamente'),\r\n+                  ),\r\n+                ],\r\n+              ),\r\n+            ),\r\n+          );\r\n+        }\r\n+\r\n+        if (!snapshot.hasData) {\r\n+          return const Scaffold(\r\n+            body: Center(child: Text('Tela não encontrada')),\r\n+          );\r\n+        }\r\n+\r\n+        final telaConfig = snapshot.data!;\r\n+\r\n+        return GenericMobileGridScreen<T>(\r\n+          title: telaConfig.titulo,\r\n+          fetchEndpoint: telaConfig.fetchEndpoint,\r\n+          createEndpoint: telaConfig.createEndpoint,\r\n+          updateEndpoint: telaConfig.updateEndpoint,\r\n+          deleteEndpoint: telaConfig.deleteEndpoint,\r\n+          fromJson: widget.fromJson,\r\n+          toJson: widget.toJson,\r\n+          hasPermission: widget.hasPermission,\r\n+          fieldConfigs: _convertToFieldConfigs(telaConfig.fields),\r\n+          idFieldName: telaConfig.idFieldName,\r\n+          dateFieldName: telaConfig.dateFieldName,\r\n+          storageKey: widget.storageKey ??\r\n+              telaConfig.storageKey ??\r\n+              'dynamic_grid_${telaConfig.nome}',\r\n+          onItemTap: widget.onItemTap,\r\n+          detailScreenBuilder: widget.detailScreenBuilder,\r\n+          extraParams: widget.extraParams,\r\n+          enableSearch: telaConfig.enableSearch,\r\n+          enableDebugMode: telaConfig.enableDebugMode,\r\n+          useUserBannerAppBar: telaConfig.useUserBannerAppBar,\r\n+          onUserBannerTapped: widget.onUserBannerTapped,\r\n+          onBannerRefresh: widget.onBannerRefresh,\r\n+          additionalFormData: widget.additionalFormData,\r\n+          dynamicAdditionalFormData: widget.dynamicAdditionalFormData,\r\n+        );\r\n+      },\r\n+    );\r\n+  }\r\n+}\r\n"
                },
                {
                    "date": 1759784649724,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -4,9 +4,9 @@\n import 'package:task_manager_flutter/data/services/tela_caller.dart';\r\n \r\n import '../models/telas_model.dart';\r\n \r\n-class DynamicGridScreen<T> extends StatefulWidget {\r\n+class DynamicGridDynamicScreen<T> extends StatefulWidget {\r\n   final String telaNome;\r\n   final FromJson<T> fromJson;\r\n   final ToJson<T> toJson;\r\n   final SecurityCheck hasPermission;\r\n@@ -18,9 +18,9 @@\n   final VoidCallback? onBannerRefresh;\r\n   final Map<String, dynamic>? additionalFormData;\r\n   final Map<String, dynamic> Function(T? item)? dynamicAdditionalFormData;\r\n \r\n-  const DynamicGridScreen({\r\n+  const DynamicGridDynamicScreen({\r\n     super.key,\r\n     required this.telaNome,\r\n     required this.fromJson,\r\n     required this.toJson,\r\n@@ -35,12 +35,14 @@\n     this.dynamicAdditionalFormData,\r\n   });\r\n \r\n   @override\r\n-  State<DynamicGridScreen<T>> createState() => _DynamicGridScreenState<T>();\r\n+  State<DynamicGridDynamicScreen<T>> createState() =>\r\n+      _DynamicGridDynamicScreenState<T>();\r\n }\r\n \r\n-class _DynamicGridScreenState<T> extends State<DynamicGridScreen<T>> {\r\n+class _DynamicGridDynamicScreenState<T>\r\n+    extends State<DynamicGridDynamicScreen<T>> {\r\n   late Future<TelaConfig> _telaFuture;\r\n   late TelaService _telaService;\r\n \r\n   @override\r\n"
                },
                {
                    "date": 1759846239705,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -52,23 +52,20 @@\n     _telaFuture = _loadTelaConfig();\r\n   }\r\n \r\n   Future<TelaConfig> _loadTelaConfig() async {\r\n-    // Tenta carregar do cache primeiro\r\n-    final cachedTela = await _telaService.getTelaFromCache(widget.telaNome);\r\n-    if (cachedTela != null) {\r\n-      return cachedTela;\r\n-    }\r\n-\r\n-    // Se não encontrou no cache, busca da API\r\n-    final tela = await _telaService.getTelaByNome(widget.telaNome);\r\n-    if (tela != null) {\r\n-      await _telaService.saveTelaToCache(widget.telaNome, tela);\r\n-      return tela;\r\n-    }\r\n-\r\n-    throw Exception('Tela ${widget.telaNome} não encontrada');\r\n+  print('🔄 Carregando configuração da tela: ${widget.telaNome}');\r\n+  \r\n+  // Tenta do cache (que já tem fallback para API)\r\n+  final tela = await _telaService.getTelaFromCache(widget.telaNome);\r\n+  \r\n+  if (tela != null) {\r\n+    print('✅ Configuração carregada com sucesso');\r\n+    return tela;\r\n   }\r\n+  \r\n+  throw Exception('Tela ${widget.telaNome} não encontrada no cache nem na API');\r\n+}\r\n \r\n   List<FieldConfig> _convertToFieldConfigs(List<TelaField> fields,\r\n       {bool forInsert = true, bool forUpdate = false}) {\r\n     return fields.where((field) {\r\n"
                },
                {
                    "date": 1759875380056,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -112,8 +112,9 @@\n                 fileFieldName: field.fileFieldName,\r\n               )\r\n             : null,\r\n         dropdownSelectedValue: field.dropdownSelectedValue,\r\n+        dropdownSelectedValue: 0, // Valor padrão selecionado\r\n         showInCard: field.showInCard,\r\n         firstDate: field.firstDate,\r\n         lastDate: field.lastDate,\r\n         dateFormat: field.dateFormat,\r\n"
                },
                {
                    "date": 1759875388848,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -111,10 +111,9 @@\n                 maxFileSize: field.maxFileSize,\r\n                 fileFieldName: field.fileFieldName,\r\n               )\r\n             : null,\r\n-        dropdownSelectedValue: field.dropdownSelectedValue,\r\n-        dropdownSelectedValue: 0, // Valor padrão selecionado\r\n+        dropdownSelectedValue: field.dropdownSelectedValue ?? 0,\r\n         showInCard: field.showInCard,\r\n         firstDate: field.firstDate,\r\n         lastDate: field.lastDate,\r\n         dateFormat: field.dateFormat,\r\n"
                },
                {
                    "date": 1759876007194,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -73,8 +73,40 @@\n       if (forInsert && !field.showInInsert) return false;\r\n       if (forUpdate && !field.showInUpdate) return false;\r\n       return field.isInForm;\r\n     }).map((field) {\r\n+      \r\n+      // **CORREÇÃO: Garantir que dropdownOptions seja único e válido**\r\n+      List<Map<String, dynamic>>? dropdownOptions;\r\n+      if (field.dropdownOptions.isNotEmpty) {\r\n+        // Remove duplicatas baseado no valor\r\n+        final uniqueOptions = <String, Map<String, dynamic>>{};\r\n+        for (final option in field.dropdownOptions) {\r\n+          final value = option.optionValue?.toString() ?? '';\r\n+          if (value.isNotEmpty && !uniqueOptions.containsKey(value)) {\r\n+            uniqueOptions[value] = {\r\n+              'value': option.optionValue,\r\n+              'label': option.optionLabel ?? option.optionValue?.toString() ?? '',\r\n+            };\r\n+          }\r\n+        }\r\n+        dropdownOptions = uniqueOptions.values.toList();\r\n+        \r\n+        // Debug para verificar as opções\r\n+        print('Dropdown ${field.fieldName}: ${dropdownOptions.length} opções únicas');\r\n+      }\r\n+\r\n+      // **CORREÇÃO: Tratar dropdownSelectedValue para evitar valores inválidos**\r\n+      dynamic selectedValue = field.dropdownSelectedValue;\r\n+      if (selectedValue != null && dropdownOptions != null) {\r\n+        final valueExists = dropdownOptions.any((option) => \r\n+            option['value']?.toString() == selectedValue?.toString());\r\n+        if (!valueExists) {\r\n+          print('⚠️ Valor selecionado $selectedValue não existe nas opções para ${field.fieldName}');\r\n+          selectedValue = null;\r\n+        }\r\n+      }\r\n+\r\n       return FieldConfig(\r\n         label: field.label,\r\n         fieldName: field.fieldName,\r\n         displayFieldName: field.displayFieldName,\r\n@@ -84,16 +116,9 @@\n         maxLines: field.maxLines,\r\n         icon: field.iconData,\r\n         isSortable: field.isSortable,\r\n         fieldType: field.fieldType,\r\n-        dropdownOptions: field.dropdownOptions.isNotEmpty\r\n-            ? field.dropdownOptions\r\n-                .map((option) => {\r\n-                      'value': option.optionValue,\r\n-                      'label': option.optionLabel,\r\n-                    })\r\n-                .toList()\r\n-            : null,\r\n+        dropdownOptions: dropdownOptions, // Usa a lista tratada\r\n         dropdownFutureBuilder: field.dropdownEndpoint != null\r\n             ? _createDropdownFutureBuilder(field.dropdownEndpoint!)\r\n             : null,\r\n         dropdownValueField: field.dropdownValueField,\r\n@@ -111,9 +136,9 @@\n                 maxFileSize: field.maxFileSize,\r\n                 fileFieldName: field.fileFieldName,\r\n               )\r\n             : null,\r\n-        dropdownSelectedValue: field.dropdownSelectedValue ?? 0,\r\n+        dropdownSelectedValue: selectedValue, // Usa o valor tratado\r\n         showInCard: field.showInCard,\r\n         firstDate: field.firstDate,\r\n         lastDate: field.lastDate,\r\n         dateFormat: field.dateFormat,\r\n@@ -128,14 +153,30 @@\n         final response = await NetworkCaller().getRequest(endpoint);\r\n         if (response.isSuccess && response.body != null) {\r\n           final data = response.body!['data'] ?? response.body!;\r\n           if (data is List) {\r\n-            return data.cast<Map<String, dynamic>>();\r\n+            final List<Map<String, dynamic>> items = data.cast<Map<String, dynamic>>();\r\n+            \r\n+            // **CORREÇÃO: Remove itens duplicados e garante valores únicos**\r\n+            final uniqueItems = <String, Map<String, dynamic>>{};\r\n+            for (final item in items) {\r\n+              final value = item['value']?.toString() ?? item['id']?.toString() ?? '';\r\n+              if (value.isNotEmpty && !uniqueItems.containsKey(value)) {\r\n+                uniqueItems[value] = {\r\n+                  'value': item['value'] ?? item['id'],\r\n+                  'label': item['label'] ?? item['name'] ?? item['value']?.toString() ?? 'Sem label',\r\n+                };\r\n+              }\r\n+            }\r\n+            \r\n+            print('✅ Dropdown carregado: ${uniqueItems.length} itens únicos de $endpoint');\r\n+            return uniqueItems.values.toList();\r\n           }\r\n         }\r\n+        print('⚠️ Dropdown vazio ou erro na resposta de $endpoint');\r\n         return [];\r\n       } catch (e) {\r\n-        print('Erro ao carregar dropdown: $e');\r\n+        print('❌ Erro ao carregar dropdown de $endpoint: $e');\r\n         return [];\r\n       }\r\n     };\r\n   }\r\n@@ -235,8 +276,11 @@\n         }\r\n \r\n         final telaConfig = snapshot.data!;\r\n \r\n+        // **CORREÇÃO: Debug para verificar os campos com dropdown**\r\n+        _debugDropdownFields(telaConfig.fields);\r\n+\r\n         return GenericMobileGridScreen<T>(\r\n           title: telaConfig.titulo,\r\n           fetchEndpoint: telaConfig.fetchEndpoint,\r\n           createEndpoint: telaConfig.createEndpoint,\r\n@@ -264,5 +308,35 @@\n         );\r\n       },\r\n     );\r\n   }\r\n-}\r\n+\r\n+  // **NOVO MÉTODO: Debug para campos dropdown**\r\n+  void _debugDropdownFields(List<TelaField> fields) {\r\n+    for (final field in fields) {\r\n+      if (field.dropdownOptions.isNotEmpty || field.dropdownEndpoint != null) {\r\n+        print('🔍 Dropdown Field: ${field.fieldName}');\r\n+        print('   - Tipo: ${field.fieldType}');\r\n+        print('   - Opções locais: ${field.dropdownOptions.length}');\r\n+        print('   - Endpoint: ${field.dropdownEndpoint}');\r\n+        print('   - Valor selecionado: ${field.dropdownSelectedValue}');\r\n+        \r\n+        if (field.dropdownOptions.isNotEmpty) {\r\n+          final values = field.dropdownOptions.map((o) => o.optionValue).toList();\r\n+          print('   - Valores disponíveis: $values');\r\n+          \r\n+          // Verifica duplicatas\r\n+          final valueSet = <dynamic>{};\r\n+          final duplicates = <dynamic>[];\r\n+          for (final value in values) {\r\n+            if (!valueSet.add(value)) {\r\n+              duplicates.add(value);\r\n+            }\r\n+          }\r\n+          if (duplicates.isNotEmpty) {\r\n+            print('   ⚠️ VALORES DUPLICADOS: $duplicates');\r\n+          }\r\n+        }\r\n+      }\r\n+    }\r\n+  }\r\n+}\n\\ No newline at end of file\n"
                },
                {
                    "date": 1760895477745,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -4,27 +4,24 @@\n import 'package:task_manager_flutter/data/services/tela_caller.dart';\r\n \r\n import '../models/telas_model.dart';\r\n \r\n-class DynamicGridDynamicScreen<T> extends StatefulWidget {\r\n+class DynamicGridDynamicScreen extends StatefulWidget {\r\n   final String telaNome;\r\n-  final FromJson<T> fromJson;\r\n-  final ToJson<T> toJson;\r\n   final SecurityCheck hasPermission;\r\n   final String? storageKey;\r\n-  final OnItemTap<T>? onItemTap;\r\n-  final Widget Function(T item)? detailScreenBuilder;\r\n+  final OnItemTap? onItemTap;\r\n+  final Widget Function(Map<String, dynamic> item)? detailScreenBuilder;\r\n   final Map<String, dynamic>? extraParams;\r\n   final VoidCallback? onUserBannerTapped;\r\n   final VoidCallback? onBannerRefresh;\r\n   final Map<String, dynamic>? additionalFormData;\r\n-  final Map<String, dynamic> Function(T? item)? dynamicAdditionalFormData;\r\n+  final Map<String, dynamic> Function(Map<String, dynamic>? item)?\r\n+      dynamicAdditionalFormData;\r\n \r\n   const DynamicGridDynamicScreen({\r\n-    super.key,\r\n+    Key? key,\r\n     required this.telaNome,\r\n-    required this.fromJson,\r\n-    required this.toJson,\r\n     required this.hasPermission,\r\n     this.storageKey,\r\n     this.onItemTap,\r\n     this.detailScreenBuilder,\r\n@@ -32,17 +29,16 @@\n     this.onUserBannerTapped,\r\n     this.onBannerRefresh,\r\n     this.additionalFormData,\r\n     this.dynamicAdditionalFormData,\r\n-  });\r\n+  }) : super(key: key);\r\n \r\n   @override\r\n-  State<DynamicGridDynamicScreen<T>> createState() =>\r\n-      _DynamicGridDynamicScreenState<T>();\r\n+  State<DynamicGridDynamicScreen> createState() =>\r\n+      _DynamicGridDynamicScreenState();\r\n }\r\n \r\n-class _DynamicGridDynamicScreenState<T>\r\n-    extends State<DynamicGridDynamicScreen<T>> {\r\n+class _DynamicGridDynamicScreenState extends State<DynamicGridDynamicScreen> {\r\n   late Future<TelaConfig> _telaFuture;\r\n   late TelaService _telaService;\r\n \r\n   @override\r\n@@ -52,56 +48,54 @@\n     _telaFuture = _loadTelaConfig();\r\n   }\r\n \r\n   Future<TelaConfig> _loadTelaConfig() async {\r\n-  print('🔄 Carregando configuração da tela: ${widget.telaNome}');\r\n-  \r\n-  // Tenta do cache (que já tem fallback para API)\r\n-  final tela = await _telaService.getTelaFromCache(widget.telaNome);\r\n-  \r\n-  if (tela != null) {\r\n-    print('✅ Configuração carregada com sucesso');\r\n-    return tela;\r\n+    print('🔄 Carregando configuração da tela: ${widget.telaNome}');\r\n+\r\n+    final tela = await _telaService.getTelaFromCache(widget.telaNome);\r\n+\r\n+    if (tela != null) {\r\n+      print('✅ Configuração carregada com sucesso');\r\n+      return tela;\r\n+    }\r\n+\r\n+    throw Exception(\r\n+        'Tela ${widget.telaNome} não encontrada no cache nem na API');\r\n   }\r\n-  \r\n-  throw Exception('Tela ${widget.telaNome} não encontrada no cache nem na API');\r\n-}\r\n \r\n   List<FieldConfig> _convertToFieldConfigs(List<TelaField> fields,\r\n       {bool forInsert = true, bool forUpdate = false}) {\r\n     return fields.where((field) {\r\n       if (forInsert && !field.showInInsert) return false;\r\n       if (forUpdate && !field.showInUpdate) return false;\r\n       return field.isInForm;\r\n     }).map((field) {\r\n-      \r\n-      // **CORREÇÃO: Garantir que dropdownOptions seja único e válido**\r\n       List<Map<String, dynamic>>? dropdownOptions;\r\n       if (field.dropdownOptions.isNotEmpty) {\r\n-        // Remove duplicatas baseado no valor\r\n         final uniqueOptions = <String, Map<String, dynamic>>{};\r\n         for (final option in field.dropdownOptions) {\r\n-          final value = option.optionValue?.toString() ?? '';\r\n+          final value = option.optionValue.toString() ?? '';\r\n           if (value.isNotEmpty && !uniqueOptions.containsKey(value)) {\r\n             uniqueOptions[value] = {\r\n               'value': option.optionValue,\r\n-              'label': option.optionLabel ?? option.optionValue?.toString() ?? '',\r\n+              'label':\r\n+                  option.optionLabel ?? option.optionValue.toString() ?? '',\r\n             };\r\n           }\r\n         }\r\n         dropdownOptions = uniqueOptions.values.toList();\r\n-        \r\n-        // Debug para verificar as opções\r\n-        print('Dropdown ${field.fieldName}: ${dropdownOptions.length} opções únicas');\r\n+\r\n+        print(\r\n+            'Dropdown ${field.fieldName}: ${dropdownOptions.length} opções únicas');\r\n       }\r\n \r\n-      // **CORREÇÃO: Tratar dropdownSelectedValue para evitar valores inválidos**\r\n       dynamic selectedValue = field.dropdownSelectedValue;\r\n       if (selectedValue != null && dropdownOptions != null) {\r\n-        final valueExists = dropdownOptions.any((option) => \r\n+        final valueExists = dropdownOptions.any((option) =>\r\n             option['value']?.toString() == selectedValue?.toString());\r\n         if (!valueExists) {\r\n-          print('⚠️ Valor selecionado $selectedValue não existe nas opções para ${field.fieldName}');\r\n+          print(\r\n+              '⚠️ Valor selecionado $selectedValue não existe nas opções para ${field.fieldName}');\r\n           selectedValue = null;\r\n         }\r\n       }\r\n \r\n@@ -115,9 +109,9 @@\n         maxLines: field.maxLines,\r\n         icon: field.iconData,\r\n         isSortable: field.isSortable,\r\n         fieldType: field.fieldType,\r\n-        dropdownOptions: dropdownOptions, // Usa a lista tratada\r\n+        dropdownOptions: dropdownOptions,\r\n         dropdownFutureBuilder: field.dropdownEndpoint != null\r\n             ? _createDropdownFutureBuilder(field.dropdownEndpoint!)\r\n             : null,\r\n         dropdownValueField: field.dropdownValueField,\r\n@@ -135,9 +129,9 @@\n                 maxFileSize: field.maxFileSize,\r\n                 fileFieldName: field.fileFieldName,\r\n               )\r\n             : null,\r\n-        dropdownSelectedValue: selectedValue, // Usa o valor tratado\r\n+        dropdownSelectedValue: selectedValue,\r\n         showInCard: field.showInCard,\r\n         firstDate: field.firstDate,\r\n         lastDate: field.lastDate,\r\n         dateFormat: field.dateFormat,\r\n@@ -152,23 +146,28 @@\n         final response = await NetworkCaller().getRequest(endpoint);\r\n         if (response.isSuccess && response.body != null) {\r\n           final data = response.body!['data'] ?? response.body!;\r\n           if (data is List) {\r\n-            final List<Map<String, dynamic>> items = data.cast<Map<String, dynamic>>();\r\n-            \r\n-            // **CORREÇÃO: Remove itens duplicados e garante valores únicos**\r\n+            final List<Map<String, dynamic>> items =\r\n+                data.cast<Map<String, dynamic>>();\r\n+\r\n             final uniqueItems = <String, Map<String, dynamic>>{};\r\n             for (final item in items) {\r\n-              final value = item['value']?.toString() ?? item['id']?.toString() ?? '';\r\n+              final value =\r\n+                  item['value']?.toString() ?? item['id']?.toString() ?? '';\r\n               if (value.isNotEmpty && !uniqueItems.containsKey(value)) {\r\n                 uniqueItems[value] = {\r\n                   'value': item['value'] ?? item['id'],\r\n-                  'label': item['label'] ?? item['name'] ?? item['value']?.toString() ?? 'Sem label',\r\n+                  'label': item['label'] ??\r\n+                      item['name'] ??\r\n+                      item['value']?.toString() ??\r\n+                      'Sem label',\r\n                 };\r\n               }\r\n             }\r\n-            \r\n-            print('✅ Dropdown carregado: ${uniqueItems.length} itens únicos de $endpoint');\r\n+\r\n+            print(\r\n+                '✅ Dropdown carregado: ${uniqueItems.length} itens únicos de $endpoint');\r\n             return uniqueItems.values.toList();\r\n           }\r\n         }\r\n         print('⚠️ Dropdown vazio ou erro na resposta de $endpoint');\r\n@@ -187,12 +186,11 @@\n       if (value == null || value.isEmpty) {\r\n         return '${field.label} é obrigatório';\r\n       }\r\n \r\n-      // Validações específicas por tipo\r\n       switch (field.fieldType) {\r\n         case FieldType.email:\r\n-          if (!RegExp(r'^[\\w-\\.]+@([\\w-]+\\.)+[\\w-]{2,4}$').hasMatch(value)) {\r\n+          if (!RegExp(r'^[\\w\\-.]+@([\\w\\-]+\\.)+[\\w\\-]{2,4}$').hasMatch(value)) {\r\n             return 'Email inválido';\r\n           }\r\n           break;\r\n         case FieldType.cpf:\r\n@@ -218,15 +216,13 @@\n     };\r\n   }\r\n \r\n   bool _validateCPF(String cpf) {\r\n-    // Implementação simplificada - substitua pela validação real\r\n     final cleaned = cpf.replaceAll(RegExp(r'[^\\d]'), '');\r\n     return cleaned.length == 11;\r\n   }\r\n \r\n   bool _validateCNPJ(String cnpj) {\r\n-    // Implementação simplificada - substitua pela validação real\r\n     final cleaned = cnpj.replaceAll(RegExp(r'[^\\d]'), '');\r\n     return cleaned.length == 14;\r\n   }\r\n \r\n@@ -275,19 +271,18 @@\n         }\r\n \r\n         final telaConfig = snapshot.data!;\r\n \r\n-        // **CORREÇÃO: Debug para verificar os campos com dropdown**\r\n         _debugDropdownFields(telaConfig.fields);\r\n \r\n-        return GenericMobileGridScreen<T>(\r\n+        return GenericMobileGridScreen(\r\n           title: telaConfig.titulo,\r\n           fetchEndpoint: telaConfig.fetchEndpoint,\r\n           createEndpoint: telaConfig.createEndpoint,\r\n           updateEndpoint: telaConfig.updateEndpoint,\r\n           deleteEndpoint: telaConfig.deleteEndpoint,\r\n-          fromJson: widget.fromJson,\r\n-          toJson: widget.toJson,\r\n+          fromJson: (json) => json,\r\n+          toJson: (item) => item,\r\n           hasPermission: widget.hasPermission,\r\n           fieldConfigs: _convertToFieldConfigs(telaConfig.fields),\r\n           idFieldName: telaConfig.idFieldName,\r\n           dateFieldName: telaConfig.dateFieldName,\r\n@@ -308,23 +303,22 @@\n       },\r\n     );\r\n   }\r\n \r\n-  // **NOVO MÉTODO: Debug para campos dropdown**\r\n   void _debugDropdownFields(List<TelaField> fields) {\r\n     for (final field in fields) {\r\n       if (field.dropdownOptions.isNotEmpty || field.dropdownEndpoint != null) {\r\n         print('🔍 Dropdown Field: ${field.fieldName}');\r\n         print('   - Tipo: ${field.fieldType}');\r\n         print('   - Opções locais: ${field.dropdownOptions.length}');\r\n         print('   - Endpoint: ${field.dropdownEndpoint}');\r\n         print('   - Valor selecionado: ${field.dropdownSelectedValue}');\r\n-        \r\n+\r\n         if (field.dropdownOptions.isNotEmpty) {\r\n-          final values = field.dropdownOptions.map((o) => o.optionValue).toList();\r\n+          final values =\r\n+              field.dropdownOptions.map((o) => o.optionValue).toList();\r\n           print('   - Valores disponíveis: $values');\r\n-          \r\n-          // Verifica duplicatas\r\n+\r\n           final valueSet = <dynamic>{};\r\n           final duplicates = <dynamic>[];\r\n           for (final value in values) {\r\n             if (!valueSet.add(value)) {\r\n@@ -337,1077 +331,11 @@\n         }\r\n       }\r\n     }\r\n   }\r\n-}\n-import 'package:flutter/material.dart';\r\n-import 'package:shared_preferences/shared_preferences.dart';\r\n-import 'package:task_manager_flutter/data/customization/generic_grid_card.dart';\r\n-import 'package:task_manager_flutter/data/services/network_caller.dart';\r\n-import 'package:task_manager_flutter/data/services/tela_caller.dart';\r\n-\r\n-import '../models/telas_model.dart';\r\n-// screens/dynamic_grid_screen.dart\r\n-import 'package:flutter/material.dart';\r\n-import 'package:task_manager_flutter/ui/screens/generic_mobile_grid_screen.dart';\r\n-import 'package:task_manager_flutter/data/services/network_caller.dart';\r\n-import '../services/tela_service.dart';\r\n-import '../services/auth_service.dart';\r\n-import '../models/tela_config.dart';\r\n-\r\n-class DynamicGridScreen<T> extends StatefulWidget {\r\n-  final String telaNome;\r\n-  final FromJson<T> fromJson;\r\n-  final ToJson<T> toJson;\r\n-  final SecurityCheck hasPermission;\r\n-  final String? storageKey;\r\n-  final OnItemTap<T>? onItemTap;\r\n-  final Widget Function(T item)? detailScreenBuilder;\r\n-  final Map<String, dynamic>? extraParams;\r\n-  final VoidCallback? onUserBannerTapped;\r\n-  final VoidCallback? onBannerRefresh;\r\n-  final Map<String, dynamic>? additionalFormData;\r\n-  final Map<String, dynamic> Function(T? item)? dynamicAdditionalFormData;\r\n-\r\n-  const DynamicGridScreen({\r\n-    super.key,\r\n-    required this.telaNome,\r\n-    required this.fromJson,\r\n-    required this.toJson,\r\n-    required this.hasPermission,\r\n-    this.storageKey,\r\n-    this.onItemTap,\r\n-    this.detailScreenBuilder,\r\n-    this.extraParams,\r\n-    this.onUserBannerTapped,\r\n-    this.onBannerRefresh,\r\n-    this.additionalFormData,\r\n-    this.dynamicAdditionalFormData,\r\n-  });\r\n-\r\n-  @override\r\n-  State<DynamicGridScreen<T>> createState() => _DynamicGridScreenState<T>();\r\n }\r\n \r\n-class _DynamicGridScreenState<T> extends State<DynamicGridScreen<T>> {\r\n-  late Future<TelaConfig> _telaFuture;\r\n-  late TelaService _telaService;\r\n-\r\n-  @override\r\n-  void initState() {\r\n-    super.initState();\r\n-    _telaService = TelaService(networkCaller: NetworkCaller());\r\n-    _telaFuture = _loadTelaConfig();\r\n-  }\r\n-\r\n-  Future<TelaConfig> _loadTelaConfig() async {\r\n-    // Tenta carregar do cache primeiro\r\n-    final cachedTela = await _telaService.getTelaFromCache(widget.telaNome);\r\n-    if (cachedTela != null) {\r\n-      return cachedTela;\r\n-    }\r\n-\r\n-    // Se não encontrou no cache, busca da API\r\n-    final tela = await _telaService.getTelaByNome(widget.telaNome);\r\n-    if (tela != null) {\r\n-      await _telaService.saveTelaToCache(widget.telaNome, tela);\r\n-      return tela;\r\n-    }\r\n-\r\n-    throw Exception('Tela ${widget.telaNome} não encontrada');\r\n-  }\r\n-\r\n-  List<FieldConfig> _convertToFieldConfigs(List<TelaField> fields, {bool forInsert = true, bool forUpdate = false}) {\r\n-    return fields.where((field) {\r\n-      if (forInsert && !field.showInInsert) return false;\r\n-      if (forUpdate && !field.showInUpdate) return false;\r\n-      return field.isInForm;\r\n-    }).map((field) {\r\n-      return FieldConfig(\r\n-        label: field.label,\r\n-        fieldName: field.fieldName,\r\n-        displayFieldName: field.displayFieldName,\r\n-        isFilterable: field.isFilterable,\r\n-        isInForm: field.isInForm,\r\n-        flex: field.flex,\r\n-        maxLines: field.maxLines,\r\n-        icon: field.iconData,\r\n-        isSortable: field.isSortable,\r\n-        fieldType: field.fieldType,\r\n-        dropdownOptions: field.dropdownOptions.isNotEmpty \r\n-            ? field.dropdownOptions.map((option) => {\r\n-                'value': option.optionValue,\r\n-                'label': option.optionLabel,\r\n-              }).toList()\r\n-            : null,\r\n-        dropdownFutureBuilder: field.dropdownEndpoint != null \r\n-            ? _createDropdownFutureBuilder(field.dropdownEndpoint!)\r\n-            : null,\r\n-        dropdownValueField: field.dropdownValueField,\r\n-        dropdownDisplayField: field.dropdownDisplayField,\r\n-        isRequired: field.isRequired,\r\n-        validator: _createValidator(field),\r\n-        isVisibleByDefault: field.isVisibleByDefault,\r\n-        isFixed: field.isFixed,\r\n-        enabled: field.enabled,\r\n-        defaultValue: field.defaultValue,\r\n-        fileConfig: field.fieldType == FieldType.file \r\n-            ? FileConfig(\r\n-                allowedExtensions: field.allowedExtensions,\r\n-                allowMultiple: field.allowMultipleFiles,\r\n-                maxFileSize: field.maxFileSize,\r\n-                fileFieldName: field.fileFieldName,\r\n-              )\r\n-            : null,\r\n-        dropdownSelectedValue: field.dropdownSelectedValue,\r\n-        showInCard: field.showInCard,\r\n-        firstDate: field.firstDate,\r\n-        lastDate: field.lastDate,\r\n-        dateFormat: field.dateFormat,\r\n-      );\r\n-    }).toList();\r\n-  }\r\n-\r\n-  Future<List<Map<String, dynamic>>> Function()? _createDropdownFutureBuilder(String endpoint) {\r\n-    return () async {\r\n-      try {\r\n-        final response = await NetworkCaller().getRequest(endpoint);\r\n-        if (response.isSuccess && response.body != null) {\r\n-          final data = response.body!['data'] ?? response.body!;\r\n-          if (data is List) {\r\n-            return data.cast<Map<String, dynamic>>();\r\n-          }\r\n-        }\r\n-        return [];\r\n-      } catch (e) {\r\n-        print('Erro ao carregar dropdown: $e');\r\n-        return [];\r\n-      }\r\n-    };\r\n-  }\r\n-\r\n-  String? Function(String?)? _createValidator(TelaField field) {\r\n-    if (!field.isRequired) return null;\r\n-\r\n-    return (value) {\r\n-      if (value == null || value.isEmpty) {\r\n-        return '${field.label} é obrigatório';\r\n-      }\r\n-      \r\n-      // Validações específicas por tipo\r\n-      switch (field.fieldType) {\r\n-        case FieldType.email:\r\n-          if (!RegExp(r'^[\\w-\\.]+@([\\w-]+\\.)+[\\w-]{2,4}$').hasMatch(value)) {\r\n-            return 'Email inválido';\r\n-          }\r\n-          break;\r\n-        case FieldType.cpf:\r\n-          if (!_validateCPF(value)) {\r\n-            return 'CPF inválido';\r\n-          }\r\n-          break;\r\n-        case FieldType.cnpj:\r\n-          if (!_validateCNPJ(value)) {\r\n-            return 'CNPJ inválido';\r\n-          }\r\n-          break;\r\n-        case FieldType.phone:\r\n-          if (!RegExp(r'^\\(\\d{2}\\)\\s\\d{4,5}-\\d{4}$').hasMatch(value)) {\r\n-            return 'Telefone inválido';\r\n-          }\r\n-          break;\r\n-        default:\r\n-          break;\r\n-      }\r\n-      \r\n-      return null;\r\n-    };\r\n-  }\r\n-\r\n-  bool _validateCPF(String cpf) {\r\n-    // Implementação simplificada - substitua pela validação real\r\n-    final cleaned = cpf.replaceAll(RegExp(r'[^\\d]'), '');\r\n-    return cleaned.length == 11;\r\n-  }\r\n-\r\n-  bool _validateCNPJ(String cnpj) {\r\n-    // Implementação simplificada - substitua pela validação real\r\n-    final cleaned = cnpj.replaceAll(RegExp(r'[^\\d]'), '');\r\n-    return cleaned.length == 14;\r\n-  }\r\n-\r\n-  @override\r\n-  Widget build(BuildContext context) {\r\n-    return FutureBuilder<TelaConfig>(\r\n-      future: _telaFuture,\r\n-      builder: (context, snapshot) {\r\n-        if (snapshot.connectionState == ConnectionState.waiting) {\r\n-          return const Scaffold(\r\n-            body: Center(child: CircularProgressIndicator()),\r\n-          );\r\n-        }\r\n-\r\n-        if (snapshot.hasError) {\r\n-          return Scaffold(\r\n-            appBar: AppBar(title: const Text('Erro')),\r\n-            body: Center(\r\n-              child: Column(\r\n-                mainAxisAlignment: MainAxisAlignment.center,\r\n-                children: [\r\n-                  const Icon(Icons.error_outline, size: 64, color: Colors.red),\r\n-                  const SizedBox(height: 16),\r\n-                  Text(\r\n-                    'Erro ao carregar tela: ${snapshot.error}',\r\n-                    textAlign: TextAlign.center,\r\n-                    style: const TextStyle(fontSize: 16),\r\n-                  ),\r\n-                  const SizedBox(height: 16),\r\n-                  ElevatedButton(\r\n-                    onPressed: () => setState(() {\r\n-                      _telaFuture = _loadTelaConfig();\r\n-                    }),\r\n-                    child: const Text('Tentar Novamente'),\r\n-                  ),\r\n-                ],\r\n-              ),\r\n-            ),\r\n-          );\r\n-        }\r\n-\r\n-        if (!snapshot.hasData) {\r\n-          return const Scaffold(\r\n-            body: Center(child: Text('Tela não encontrada')),\r\n-          );\r\n-        }\r\n-\r\n-        final telaConfig = snapshot.data!;\r\n-\r\n-        return GenericMobileGridScreen<T>(\r\n-          title: telaConfig.titulo,\r\n-          fetchEndpoint: telaConfig.fetchEndpoint,\r\n-          createEndpoint: telaConfig.createEndpoint,\r\n-          updateEndpoint: telaConfig.updateEndpoint,\r\n-          deleteEndpoint: telaConfig.deleteEndpoint,\r\n-          fromJson: widget.fromJson,\r\n-          toJson: widget.toJson,\r\n-          hasPermission: widget.hasPermission,\r\n-          fieldConfigs: _convertToFieldConfigs(telaConfig.fields),\r\n-          idFieldName: telaConfig.idFieldName,\r\n-          dateFieldName: telaConfig.dateFieldName,\r\n-          storageKey: widget.storageKey ?? telaConfig.storageKey ?? 'dynamic_grid_${telaConfig.nome}',\r\n-          onItemTap: widget.onItemTap,\r\n-          detailScreenBuilder: widget.detailScreenBuilder,\r\n-          extraParams: widget.extraParams,\r\n-          enableSearch: telaConfig.enableSearch,\r\n-          enableDebugMode: telaConfig.enableDebugMode,\r\n-          useUserBannerAppBar: telaConfig.useUserBannerAppBar,\r\n-          onUserBannerTapped: widget.onUserBannerTapped,\r\n-          onBannerRefresh: widget.onBannerRefresh,\r\n-          additionalFormData: widget.additionalFormData,\r\n-          dynamicAdditionalFormData: widget.dynamicAdditionalFormData,\r\n-        );\r\n-      },\r\n-    );\r\n-  }\r\n-}\n-import 'package:file_picker/file_picker.dart';\r\n-import 'package:flutter/material.dart';\r\n-import 'package:intl/intl.dart';\r\n-import 'package:shared_preferences/shared_preferences.dart';\r\n-import 'package:task_manager_flutter/data/models/network_response.dart';\r\n-import 'package:task_manager_flutter/data/services/network_caller.dart';\r\n-import 'package:task_manager_flutter/data/services/upload_file_caller.dart';\r\n-import 'package:task_manager_flutter/data/services/tela_caller.dart';\r\n-import 'package:task_manager_flutter/ui/widgets/user_banners.dart';\r\n-import 'package:task_manager_flutter/data/customization/generic_grid_card.dart';\r\n-import '../models/telas_model.dart';\r\n-// ==============================================\r\n-// MOBILE GRID SCREEN - MATERIAL DESIGN 3 COMPLETO\r\n-// ==============================================\r\n-\r\n-// screens/dynamic_grid_screen.dart\r\n-class DynamicGridScreen<T> extends StatefulWidget {\r\n-  final String telaNome;\r\n-  final FromJson<T> fromJson;\r\n-  final ToJson<T> toJson;\r\n-  final SecurityCheck hasPermission;\r\n-  final String? storageKey;\r\n-  final OnItemTap<T>? onItemTap;\r\n-  final Widget Function(T item)? detailScreenBuilder;\r\n-  final Map<String, dynamic>? extraParams;\r\n-  final VoidCallback? onUserBannerTapped;\r\n-  final VoidCallback? onBannerRefresh;\r\n-  final Map<String, dynamic>? additionalFormData;\r\n-  final Map<String, dynamic> Function(T? item)? dynamicAdditionalFormData;\r\n-\r\n-  const DynamicGridScreen({\r\n-    super.key,\r\n-    required this.telaNome,\r\n-    required this.fromJson,\r\n-    required this.toJson,\r\n-    required this.hasPermission,\r\n-    this.storageKey,\r\n-    this.onItemTap,\r\n-    this.detailScreenBuilder,\r\n-    this.extraParams,\r\n-    this.onUserBannerTapped,\r\n-    this.onBannerRefresh,\r\n-    this.additionalFormData,\r\n-    this.dynamicAdditionalFormData,\r\n-  });\r\n-\r\n-  @override\r\n-  State<DynamicGridScreen<T>> createState() => _DynamicGridScreenState<T>();\r\n-}\r\n-\r\n-class _DynamicGridScreenState<T> extends State<DynamicGridScreen<T>> {\r\n-  late Future<TelaConfig> _telaFuture;\r\n-  final TelaService _telaService = TelaService(\r\n-    networkCaller: NetworkCaller(),\r\n-    prefs: SharedPreferences.getInstance() as SharedPreferences,\r\n-  );\r\n-\r\n-  @override\r\n-  void initState() {\r\n-    super.initState();\r\n-    _telaFuture = _loadTelaConfig();\r\n-  }\r\n-\r\n-  Future<TelaConfig> _loadTelaConfig() async {\r\n-    // Tenta carregar do cache primeiro\r\n-    final cachedTela = await _telaService.getTelaFromCache(widget.telaNome);\r\n-    if (cachedTela != null) {\r\n-      return cachedTela;\r\n-    }\r\n-\r\n-    // Se não encontrou no cache, busca da API\r\n-    final tela = await _telaService.getTelaByNome(widget.telaNome);\r\n-    if (tela != null) {\r\n-      await _telaService.saveTelaToCache(widget.telaNome, tela);\r\n-      return tela;\r\n-    }\r\n-\r\n-    throw Exception('Tela ${widget.telaNome} não encontrada');\r\n-  }\r\n-\r\n-  List<FieldConfig> _convertToFieldConfigs(List<TelaField> fields,\r\n-      {bool forInsert = true, bool forUpdate = false}) {\r\n-    return fields.where((field) {\r\n-      if (forInsert && !field.showInInsert) return false;\r\n-      if (forUpdate && !field.showInUpdate) return false;\r\n-      return field.isInForm;\r\n-    }).map((field) {\r\n-      return FieldConfig(\r\n-        label: field.label,\r\n-        fieldName: field.fieldName,\r\n-        displayFieldName: field.displayFieldName,\r\n-        isFilterable: field.isFilterable,\r\n-        isInForm: field.isInForm,\r\n-        flex: field.flex,\r\n-        maxLines: field.maxLines,\r\n-        icon: field.iconData,\r\n-        isSortable: field.isSortable,\r\n-        fieldType: field.fieldType,\r\n-        dropdownOptions: field.dropdownOptionsMap,\r\n-        dropdownFutureBuilder: field.dropdownEndpoint != null\r\n-            ? _createDropdownFutureBuilder(field.dropdownEndpoint!)\r\n-            : null,\r\n-        dropdownValueField: field.dropdownValueField,\r\n-        dropdownDisplayField: field.dropdownDisplayField,\r\n-        isRequired: field.isRequired,\r\n-        validator: _createValidator(field),\r\n-        isVisibleByDefault: field.isVisibleByDefault,\r\n-        isFixed: field.isFixed,\r\n-        enabled: field.enabled,\r\n-        defaultValue: field.defaultValue,\r\n-        fileConfig: field.fieldType == FieldType.file ? field.fileConfig : null,\r\n-        dropdownSelectedValue: field.dropdownSelectedValue,\r\n-        showInCard: field.showInCard,\r\n-        firstDate: field.firstDate,\r\n-        lastDate: field.lastDate,\r\n-        dateFormat: field.dateFormat,\r\n-      );\r\n-    }).toList();\r\n-  }\r\n-\r\n-  Future<List<Map<String, dynamic>>> Function()? _createDropdownFutureBuilder(\r\n-      String endpoint) {\r\n-    return () async {\r\n-      try {\r\n-        final response = await NetworkCaller().getRequest(endpoint);\r\n-        if (response.isSuccess && response.body != null) {\r\n-          final data = response.body!['data'] ?? response.body!;\r\n-          return (data as List).cast<Map<String, dynamic>>();\r\n-        }\r\n-        return [];\r\n-      } catch (e) {\r\n-        print('Erro ao carregar dropdown: $e');\r\n-        return [];\r\n-      }\r\n-    };\r\n-  }\r\n-\r\n-  String? Function(String?)? _createValidator(TelaField field) {\r\n-    if (!field.isRequired) return null;\r\n-\r\n-    return (value) {\r\n-      if (value == null || value.isEmpty) {\r\n-        return '${field.label} é obrigatório';\r\n-      }\r\n-\r\n-      // Validações específicas por tipo\r\n-      switch (field.fieldType) {\r\n-        case FieldType.email:\r\n-          if (!RegExp(r'^[\\w-\\.]+@([\\w-]+\\.)+[\\w-]{2,4}$').hasMatch(value)) {\r\n-            return 'Email inválido';\r\n-          }\r\n-          break;\r\n-        case FieldType.cpf:\r\n-          if (!_validateCPF(value)) {\r\n-            return 'CPF inválido';\r\n-          }\r\n-          break;\r\n-        case FieldType.cnpj:\r\n-          if (!_validateCNPJ(value)) {\r\n-            return 'CNPJ inválido';\r\n-          }\r\n-          break;\r\n-        case FieldType.phone:\r\n-          if (!RegExp(r'^\\(\\d{2}\\)\\s\\d{4,5}-\\d{4}$').hasMatch(value)) {\r\n-            return 'Telefone inválido';\r\n-          }\r\n-          break;\r\n-        default:\r\n-          break;\r\n-      }\r\n-\r\n-      return null;\r\n-    };\r\n-  }\r\n-\r\n-  bool _validateCPF(String cpf) {\r\n-    // Implementação da validação de CPF\r\n-    return true;\r\n-  }\r\n-\r\n-  bool _validateCNPJ(String cnpj) {\r\n-    // Implementação da validação de CNPJ\r\n-    return true;\r\n-  }\r\n-\r\n-  @override\r\n-  Widget build(BuildContext context) {\r\n-    return FutureBuilder<TelaConfig>(\r\n-      future: _telaFuture,\r\n-      builder: (context, snapshot) {\r\n-        if (snapshot.connectionState == ConnectionState.waiting) {\r\n-          return const Scaffold(\r\n-            body: Center(child: CircularProgressIndicator()),\r\n-          );\r\n-        }\r\n-\r\n-        if (snapshot.hasError) {\r\n-          return Scaffold(\r\n-            appBar: AppBar(title: const Text('Erro')),\r\n-            body: Center(\r\n-              child: Column(\r\n-                mainAxisAlignment: MainAxisAlignment.center,\r\n-                children: [\r\n-                  const Icon(Icons.error_outline, size: 64, color: Colors.red),\r\n-                  const SizedBox(height: 16),\r\n-                  Text(\r\n-                    'Erro ao carregar tela: ${snapshot.error}',\r\n-                    textAlign: TextAlign.center,\r\n-                    style: const TextStyle(fontSize: 16),\r\n-                  ),\r\n-                  const SizedBox(height: 16),\r\n-                  ElevatedButton(\r\n-                    onPressed: () => setState(() {\r\n-                      _telaFuture = _loadTelaConfig();\r\n-                    }),\r\n-                    child: const Text('Tentar Novamente'),\r\n-                  ),\r\n-                ],\r\n-              ),\r\n-            ),\r\n-          );\r\n-        }\r\n-\r\n-        if (!snapshot.hasData) {\r\n-          return const Scaffold(\r\n-            body: Center(child: Text('Tela não encontrada')),\r\n-          );\r\n-        }\r\n-\r\n-        final telaConfig = snapshot.data!;\r\n-\r\n-        return GenericMobileGridScreen<T>(\r\n-          title: telaConfig.titulo,\r\n-          fetchEndpoint: telaConfig.fetchEndpoint,\r\n-          createEndpoint: telaConfig.createEndpoint,\r\n-          updateEndpoint: telaConfig.updateEndpoint,\r\n-          deleteEndpoint: telaConfig.deleteEndpoint,\r\n-          fromJson: widget.fromJson,\r\n-          toJson: widget.toJson,\r\n-          hasPermission: widget.hasPermission,\r\n-          fieldConfigs: _convertToFieldConfigs(telaConfig.fields),\r\n-          idFieldName: telaConfig.idFieldName,\r\n-          dateFieldName: telaConfig.dateFieldName,\r\n-          storageKey: widget.storageKey ??\r\n-              telaConfig.storageKey ??\r\n-              'dynamic_grid_${telaConfig.nome}',\r\n-          onItemTap: widget.onItemTap,\r\n-          detailScreenBuilder: widget.detailScreenBuilder,\r\n-          extraParams: widget.extraParams,\r\n-          enableSearch: telaConfig.enableSearch,\r\n-          enableDebugMode: telaConfig.enableDebugMode,\r\n-          useUserBannerAppBar: telaConfig.useUserBannerAppBar,\r\n-          onUserBannerTapped: widget.onUserBannerTapped,\r\n-          onBannerRefresh: widget.onBannerRefresh,\r\n-          additionalFormData: widget.additionalFormData,\r\n-          dynamicAdditionalFormData: widget.dynamicAdditionalFormData,\r\n-        );\r\n-      },\r\n-    );\r\n-  }\r\n-}\r\n-import 'package:file_picker/file_picker.dart';\r\n-import 'package:flutter/material.dart';\r\n-import 'package:intl/intl.dart';\r\n-import 'package:shared_preferences/shared_preferences.dart';\r\n-import 'package:task_manager_flutter/data/models/network_response.dart';\r\n-import 'package:task_manager_flutter/data/services/network_caller.dart';\r\n-import 'package:task_manager_flutter/data/services/upload_file_caller.dart';\r\n-import 'package:task_manager_flutter/ui/widgets/user_banners.dart';\r\n-import 'package:task_manager_flutter/data/customization/generic_grid_card.dart';\r\n-import '../models/telas_model.dart';\r\n-// ==============================================\r\n-// MOBILE GRID SCREEN - MATERIAL DESIGN 3 COMPLETO\r\n-// ==============================================\r\n-\r\n-// screens/dynamic_grid_screen.dart\r\n-class DynamicGridScreen<T> extends StatefulWidget {\r\n-  final String telaNome;\r\n-  final FromJson<T> fromJson;\r\n-  final ToJson<T> toJson;\r\n-  final SecurityCheck hasPermission;\r\n-  final String? storageKey;\r\n-  final OnItemTap<T>? onItemTap;\r\n-  final Widget Function(T item)? detailScreenBuilder;\r\n-  final Map<String, dynamic>? extraParams;\r\n-  final VoidCallback? onUserBannerTapped;\r\n-  final VoidCallback? onBannerRefresh;\r\n-  final Map<String, dynamic>? additionalFormData;\r\n-  final Map<String, dynamic> Function(T? item)? dynamicAdditionalFormData;\r\n-\r\n-  const DynamicGridScreen({\r\n-    super.key,\r\n-    required this.telaNome,\r\n-    required this.fromJson,\r\n-    required this.toJson,\r\n-    required this.hasPermission,\r\n-    this.storageKey,\r\n-    this.onItemTap,\r\n-    this.detailScreenBuilder,\r\n-    this.extraParams,\r\n-    this.onUserBannerTapped,\r\n-    this.onBannerRefresh,\r\n-    this.additionalFormData,\r\n-    this.dynamicAdditionalFormData,\r\n-  });\r\n-\r\n-  @override\r\n-  State<DynamicGridScreen<T>> createState() => _DynamicGridScreenState<T>();\r\n-}\r\n-\r\n-class _DynamicGridScreenState<T> extends State<DynamicGridScreen<T>> {\r\n-  late Future<TelaConfig> _telaFuture;\r\n-  final TelaService _telaService = TelaService(\r\n-    networkCaller: NetworkCaller(),\r\n-    prefs: SharedPreferences.getInstance() as SharedPreferences,\r\n-  );\r\n-\r\n-  @override\r\n-  void initState() {\r\n-    super.initState();\r\n-    _telaFuture = _loadTelaConfig();\r\n-  }\r\n-\r\n-  Future<TelaConfig> _loadTelaConfig() async {\r\n-    // Tenta carregar do cache primeiro\r\n-    final cachedTela = await _telaService.getTelaFromCache(widget.telaNome);\r\n-    if (cachedTela != null) {\r\n-      return cachedTela;\r\n-    }\r\n-\r\n-    // Se não encontrou no cache, busca da API\r\n-    final tela = await _telaService.getTelaByNome(widget.telaNome);\r\n-    if (tela != null) {\r\n-      await _telaService.saveTelaToCache(widget.telaNome, tela);\r\n-      return tela;\r\n-    }\r\n-\r\n-    throw Exception('Tela ${widget.telaNome} não encontrada');\r\n-  }\r\n-\r\n-  List<FieldConfig> _convertToFieldConfigs(List<TelaField> fields,\r\n-      {bool forInsert = true, bool forUpdate = false}) {\r\n-    return fields.where((field) {\r\n-      if (forInsert && !field.showInInsert) return false;\r\n-      if (forUpdate && !field.showInUpdate) return false;\r\n-      return field.isInForm;\r\n-    }).map((field) {\r\n-      return FieldConfig(\r\n-        label: field.label,\r\n-        fieldName: field.fieldName,\r\n-        displayFieldName: field.displayFieldName,\r\n-        isFilterable: field.isFilterable,\r\n-        isInForm: field.isInForm,\r\n-        flex: field.flex,\r\n-        maxLines: field.maxLines,\r\n-        icon: field.iconData,\r\n-        isSortable: field.isSortable,\r\n-        fieldType: field.fieldType,\r\n-        dropdownOptions: field.dropdownOptionsMap,\r\n-        dropdownFutureBuilder: field.dropdownEndpoint != null\r\n-            ? _createDropdownFutureBuilder(field.dropdownEndpoint!)\r\n-            : null,\r\n-        dropdownValueField: field.dropdownValueField,\r\n-        dropdownDisplayField: field.dropdownDisplayField,\r\n-        isRequired: field.isRequired,\r\n-        validator: _createValidator(field),\r\n-        isVisibleByDefault: field.isVisibleByDefault,\r\n-        isFixed: field.isFixed,\r\n-        enabled: field.enabled,\r\n-        defaultValue: field.defaultValue,\r\n-        fileConfig: field.fieldType == FieldType.file ? field.fileConfig : null,\r\n-        dropdownSelectedValue: field.dropdownSelectedValue,\r\n-        showInCard: field.showInCard,\r\n-        firstDate: field.firstDate,\r\n-        lastDate: field.lastDate,\r\n-        dateFormat: field.dateFormat,\r\n-      );\r\n-    }).toList();\r\n-  }\r\n-\r\n-  Future<List<Map<String, dynamic>>> Function()? _createDropdownFutureBuilder(\r\n-      String endpoint) {\r\n-    return () async {\r\n-      try {\r\n-        final response = await NetworkCaller().getRequest(endpoint);\r\n-        if (response.isSuccess && response.body != null) {\r\n-          final data = response.body!['data'] ?? response.body!;\r\n-          return (data as List).cast<Map<String, dynamic>>();\r\n-        }\r\n-        return [];\r\n-      } catch (e) {\r\n-        print('Erro ao carregar dropdown: $e');\r\n-        return [];\r\n-      }\r\n-    };\r\n-  }\r\n-\r\n-  String? Function(String?)? _createValidator(TelaField field) {\r\n-    if (!field.isRequired) return null;\r\n-\r\n-    return (value) {\r\n-      if (value == null || value.isEmpty) {\r\n-        return '${field.label} é obrigatório';\r\n-      }\r\n-\r\n-      // Validações específicas por tipo\r\n-      switch (field.fieldType) {\r\n-        case FieldType.email:\r\n-          if (!RegExp(r'^[\\w-\\.]+@([\\w-]+\\.)+[\\w-]{2,4}$').hasMatch(value)) {\r\n-            return 'Email inválido';\r\n-          }\r\n-          break;\r\n-        case FieldType.cpf:\r\n-          if (!_validateCPF(value)) {\r\n-            return 'CPF inválido';\r\n-          }\r\n-          break;\r\n-        case FieldType.cnpj:\r\n-          if (!_validateCNPJ(value)) {\r\n-            return 'CNPJ inválido';\r\n-          }\r\n-          break;\r\n-        case FieldType.phone:\r\n-          if (!RegExp(r'^\\(\\d{2}\\)\\s\\d{4,5}-\\d{4}$').hasMatch(value)) {\r\n-            return 'Telefone inválido';\r\n-          }\r\n-          break;\r\n-        default:\r\n-          break;\r\n-      }\r\n-\r\n-      return null;\r\n-    };\r\n-  }\r\n-\r\n-  bool _validateCPF(String cpf) {\r\n-    // Implementação da validação de CPF\r\n-    return true;\r\n-  }\r\n-\r\n-  bool _validateCNPJ(String cnpj) {\r\n-    // Implementação da validação de CNPJ\r\n-    return true;\r\n-  }\r\n-\r\n-  @override\r\n-  Widget build(BuildContext context) {\r\n-    return FutureBuilder<TelaConfig>(\r\n-      future: _telaFuture,\r\n-      builder: (context, snapshot) {\r\n-        if (snapshot.connectionState == ConnectionState.waiting) {\r\n-          return const Scaffold(\r\n-            body: Center(child: CircularProgressIndicator()),\r\n-          );\r\n-        }\r\n-\r\n-        if (snapshot.hasError) {\r\n-          return Scaffold(\r\n-            appBar: AppBar(title: const Text('Erro')),\r\n-            body: Center(\r\n-              child: Column(\r\n-                mainAxisAlignment: MainAxisAlignment.center,\r\n-                children: [\r\n-                  const Icon(Icons.error_outline, size: 64, color: Colors.red),\r\n-                  const SizedBox(height: 16),\r\n-                  Text(\r\n-                    'Erro ao carregar tela: ${snapshot.error}',\r\n-                    textAlign: TextAlign.center,\r\n-                    style: const TextStyle(fontSize: 16),\r\n-                  ),\r\n-                  const SizedBox(height: 16),\r\n-                  ElevatedButton(\r\n-                    onPressed: () => setState(() {\r\n-                      _telaFuture = _loadTelaConfig();\r\n-                    }),\r\n-                    child: const Text('Tentar Novamente'),\r\n-                  ),\r\n-                ],\r\n-              ),\r\n-            ),\r\n-          );\r\n-        }\r\n-\r\n-        if (!snapshot.hasData) {\r\n-          return const Scaffold(\r\n-            body: Center(child: Text('Tela não encontrada')),\r\n-          );\r\n-        }\r\n-\r\n-        final telaConfig = snapshot.data!;\r\n-\r\n-        return GenericMobileGridScreen<T>(\r\n-          title: telaConfig.titulo,\r\n-          fetchEndpoint: telaConfig.fetchEndpoint,\r\n-          createEndpoint: telaConfig.createEndpoint,\r\n-          updateEndpoint: telaConfig.updateEndpoint,\r\n-          deleteEndpoint: telaConfig.deleteEndpoint,\r\n-          fromJson: widget.fromJson,\r\n-          toJson: widget.toJson,\r\n-          hasPermission: widget.hasPermission,\r\n-          fieldConfigs: _convertToFieldConfigs(telaConfig.fields),\r\n-          idFieldName: telaConfig.idFieldName,\r\n-          dateFieldName: telaConfig.dateFieldName,\r\n-          storageKey: widget.storageKey ??\r\n-              telaConfig.storageKey ??\r\n-              'dynamic_grid_${telaConfig.nome}',\r\n-          onItemTap: widget.onItemTap,\r\n-          detailScreenBuilder: widget.detailScreenBuilder,\r\n-          extraParams: widget.extraParams,\r\n-          enableSearch: telaConfig.enableSearch,\r\n-          enableDebugMode: telaConfig.enableDebugMode,\r\n-          useUserBannerAppBar: telaConfig.useUserBannerAppBar,\r\n-          onUserBannerTapped: widget.onUserBannerTapped,\r\n-          onBannerRefresh: widget.onBannerRefresh,\r\n-          additionalFormData: widget.additionalFormData,\r\n-          dynamicAdditionalFormData: widget.dynamicAdditionalFormData,\r\n-        );\r\n-      },\r\n-    );\r\n-  }\r\n-}\r\n-import 'package:file_picker/file_picker.dart';\r\n-import 'package:flutter/material.dart';\r\n-import 'package:intl/intl.dart';\r\n-import 'package:shared_preferences/shared_preferences.dart';\r\n-import 'package:task_manager_flutter/data/models/network_response.dart';\r\n-import 'package:task_manager_flutter/data/services/network_caller.dart';\r\n-import 'package:task_manager_flutter/data/services/upload_file_caller.dart';\r\n-import 'package:task_manager_flutter/ui/widgets/user_banners.dart';\r\n-// ==============================================\r\n-// MOBILE GRID SCREEN - MATERIAL DESIGN 3 COMPLETO\r\n-// ==============================================\r\n-\r\n-class GridColors {\r\n-  static const Color primary = Color(0xFF93070A);\r\n-  static const Color primaryDark = Color(0xFF6A0507);\r\n-  static const Color primaryLight = Color(0xFFB84042);\r\n-  static const Color secondary = Color(0xFF005826);\r\n-  static const Color secondaryLight = Color(0xFF2E7D32);\r\n-  static const Color secondaryDark = Color(0xFF003D1A);\r\n-  static const Color textPrimary = Color(0xFFFFFFFF);\r\n-  static const Color textSecondary = Color(0xFF000000);\r\n-  static const Color link = Color(0xFFFF0000);\r\n-  static const Color inputBackground = Color(0xFFFFFFFF);\r\n-  static const Color inputBorder = Color(0xFF93070A);\r\n-  static const Color buttonBackground = Color(0xFF93070A);\r\n-  static const Color buttonText = Color(0xFFFFFFFF);\r\n-  static const Color background = Color(0xFF005826);\r\n-  static const Color card = Color(0xFFFFFFFF);\r\n-  static const Color error = Color(0xFFD32F2F);\r\n-  static const Color warning = Color(0xFFFFA000);\r\n-  static const Color success = Color(0xFF2E7D32);\r\n-  static const Color info = Color(0xFF1976D2);\r\n-  static const Color divider = Color(0xFFBDBDBD);\r\n-  static const Color filterBackground = Color(0xFFEFEFEF);\r\n-  static const Color hover = Color(0x1A000000);\r\n-  static const Color selectedRow = Color(0xFFE3F2FD);\r\n-  static const Color dialogBackground = Color(0xFFFFFFFF);\r\n-  static const Color shadow = Color(0x26000000);\r\n-}\r\n-// screens/dynamic_grid_screen.dart\r\n-class DynamicGridScreen<T> extends StatefulWidget {\r\n-  final String telaNome;\r\n-  final FromJson<T> fromJson;\r\n-  final ToJson<T> toJson;\r\n-  final SecurityCheck hasPermission;\r\n-  final String? storageKey;\r\n-  final OnItemTap<T>? onItemTap;\r\n-  final Widget Function(T item)? detailScreenBuilder;\r\n-  final Map<String, dynamic>? extraParams;\r\n-  final VoidCallback? onUserBannerTapped;\r\n-  final VoidCallback? onBannerRefresh;\r\n-  final Map<String, dynamic>? additionalFormData;\r\n-  final Map<String, dynamic> Function(T? item)? dynamicAdditionalFormData;\r\n-\r\n-  const DynamicGridScreen({\r\n-    super.key,\r\n-    required this.telaNome,\r\n-    required this.fromJson,\r\n-    required this.toJson,\r\n-    required this.hasPermission,\r\n-    this.storageKey,\r\n-    this.onItemTap,\r\n-    this.detailScreenBuilder,\r\n-    this.extraParams,\r\n-    this.onUserBannerTapped,\r\n-    this.onBannerRefresh,\r\n-    this.additionalFormData,\r\n-    this.dynamicAdditionalFormData,\r\n-  });\r\n-\r\n-  @override\r\n-  State<DynamicGridScreen<T>> createState() => _DynamicGridScreenState<T>();\r\n-}\r\n-\r\n-class _DynamicGridScreenState<T> extends State<DynamicGridScreen<T>> {\r\n-  late Future<TelaConfig> _telaFuture;\r\n-  final TelaService _telaService = TelaService(\r\n-    networkCaller: NetworkCaller(),\r\n-    prefs: SharedPreferences.getInstance() as SharedPreferences,\r\n-  );\r\n-\r\n-  @override\r\n-  void initState() {\r\n-    super.initState();\r\n-    _telaFuture = _loadTelaConfig();\r\n-  }\r\n-\r\n-  Future<TelaConfig> _loadTelaConfig() async {\r\n-    // Tenta carregar do cache primeiro\r\n-    final cachedTela = await _telaService.getTelaFromCache(widget.telaNome);\r\n-    if (cachedTela != null) {\r\n-      return cachedTela;\r\n-    }\r\n-\r\n-    // Se não encontrou no cache, busca da API\r\n-    final tela = await _telaService.getTelaByNome(widget.telaNome);\r\n-    if (tela != null) {\r\n-      await _telaService.saveTelaToCache(widget.telaNome, tela);\r\n-      return tela;\r\n-    }\r\n-\r\n-    throw Exception('Tela ${widget.telaNome} não encontrada');\r\n-  }\r\n-\r\n-  List<FieldConfig> _convertToFieldConfigs(List<TelaField> fields, {bool forInsert = true, bool forUpdate = false}) {\r\n-    return fields.where((field) {\r\n-      if (forInsert && !field.showInInsert) return false;\r\n-      if (forUpdate && !field.showInUpdate) return false;\r\n-      return field.isInForm;\r\n-    }).map((field) {\r\n-      return FieldConfig(\r\n-        label: field.label,\r\n-        fieldName: field.fieldName,\r\n-        displayFieldName: field.displayFieldName,\r\n-        isFilterable: field.isFilterable,\r\n-        isInForm: field.isInForm,\r\n-        flex: field.flex,\r\n-        maxLines: field.maxLines,\r\n-        icon: field.iconData,\r\n-        isSortable: field.isSortable,\r\n-        fieldType: field.fieldType,\r\n-        dropdownOptions: field.dropdownOptionsMap,\r\n-        dropdownFutureBuilder: field.dropdownEndpoint != null \r\n-            ? _createDropdownFutureBuilder(field.dropdownEndpoint!)\r\n-            : null,\r\n-        dropdownValueField: field.dropdownValueField,\r\n-        dropdownDisplayField: field.dropdownDisplayField,\r\n-        isRequired: field.isRequired,\r\n-        validator: _createValidator(field),\r\n-        isVisibleByDefault: field.isVisibleByDefault,\r\n-        isFixed: field.isFixed,\r\n-        enabled: field.enabled,\r\n-        defaultValue: field.defaultValue,\r\n-        fileConfig: field.fieldType == FieldType.file ? field.fileConfig : null,\r\n-        dropdownSelectedValue: field.dropdownSelectedValue,\r\n-        showInCard: field.showInCard,\r\n-        firstDate: field.firstDate,\r\n-        lastDate: field.lastDate,\r\n-        dateFormat: field.dateFormat,\r\n-      );\r\n-    }).toList();\r\n-  }\r\n-\r\n-  Future<List<Map<String, dynamic>>> Function()? _createDropdownFutureBuilder(String endpoint) {\r\n-    return () async {\r\n-      try {\r\n-        final response = await NetworkCaller().getRequest(endpoint);\r\n-        if (response.isSuccess && response.body != null) {\r\n-          final data = response.body!['data'] ?? response.body!;\r\n-          return (data as List).cast<Map<String, dynamic>>();\r\n-        }\r\n-        return [];\r\n-      } catch (e) {\r\n-        print('Erro ao carregar dropdown: $e');\r\n-        return [];\r\n-      }\r\n-    };\r\n-  }\r\n-\r\n-  String? Function(String?)? _createValidator(TelaField field) {\r\n-    if (!field.isRequired) return null;\r\n-\r\n-    return (value) {\r\n-      if (value == null || value.isEmpty) {\r\n-        return '${field.label} é obrigatório';\r\n-      }\r\n-      \r\n-      // Validações específicas por tipo\r\n-      switch (field.fieldType) {\r\n-        case FieldType.email:\r\n-          if (!RegExp(r'^[\\w-\\.]+@([\\w-]+\\.)+[\\w-]{2,4}$').hasMatch(value)) {\r\n-            return 'Email inválido';\r\n-          }\r\n-          break;\r\n-        case FieldType.cpf:\r\n-          if (!_validateCPF(value)) {\r\n-            return 'CPF inválido';\r\n-          }\r\n-          break;\r\n-        case FieldType.cnpj:\r\n-          if (!_validateCNPJ(value)) {\r\n-            return 'CNPJ inválido';\r\n-          }\r\n-          break;\r\n-        case FieldType.phone:\r\n-          if (!RegExp(r'^\\(\\d{2}\\)\\s\\d{4,5}-\\d{4}$').hasMatch(value)) {\r\n-            return 'Telefone inválido';\r\n-          }\r\n-          break;\r\n-        default:\r\n-          break;\r\n-      }\r\n-      \r\n-      return null;\r\n-    };\r\n-  }\r\n-\r\n-  bool _validateCPF(String cpf) {\r\n-    // Implementação da validação de CPF\r\n-    return true;\r\n-  }\r\n-\r\n-  bool _validateCNPJ(String cnpj) {\r\n-    // Implementação da validação de CNPJ\r\n-    return true;\r\n-  }\r\n-\r\n-  @override\r\n-  Widget build(BuildContext context) {\r\n-    return FutureBuilder<TelaConfig>(\r\n-      future: _telaFuture,\r\n-      builder: (context, snapshot) {\r\n-        if (snapshot.connectionState == ConnectionState.waiting) {\r\n-          return const Scaffold(\r\n-            body: Center(child: CircularProgressIndicator()),\r\n-          );\r\n-        }\r\n-\r\n-        if (snapshot.hasError) {\r\n-          return Scaffold(\r\n-            appBar: AppBar(title: const Text('Erro')),\r\n-            body: Center(\r\n-              child: Column(\r\n-                mainAxisAlignment: MainAxisAlignment.center,\r\n-                children: [\r\n-                  const Icon(Icons.error_outline, size: 64, color: Colors.red),\r\n-                  const SizedBox(height: 16),\r\n-                  Text(\r\n-                    'Erro ao carregar tela: ${snapshot.error}',\r\n-                    textAlign: TextAlign.center,\r\n-                    style: const TextStyle(fontSize: 16),\r\n-                  ),\r\n-                  const SizedBox(height: 16),\r\n-                  ElevatedButton(\r\n-                    onPressed: () => setState(() {\r\n-                      _telaFuture = _loadTelaConfig();\r\n-                    }),\r\n-                    child: const Text('Tentar Novamente'),\r\n-                  ),\r\n-                ],\r\n-              ),\r\n-            ),\r\n-          );\r\n-        }\r\n-\r\n-        if (!snapshot.hasData) {\r\n-          return const Scaffold(\r\n-            body: Center(child: Text('Tela não encontrada')),\r\n-          );\r\n-        }\r\n-\r\n-        final telaConfig = snapshot.data!;\r\n-\r\n-        return GenericMobileGridScreen<T>(\r\n-          title: telaConfig.titulo,\r\n-          fetchEndpoint: telaConfig.fetchEndpoint,\r\n-          createEndpoint: telaConfig.createEndpoint,\r\n-          updateEndpoint: telaConfig.updateEndpoint,\r\n-          deleteEndpoint: telaConfig.deleteEndpoint,\r\n-          fromJson: widget.fromJson,\r\n-          toJson: widget.toJson,\r\n-          hasPermission: widget.hasPermission,\r\n-          fieldConfigs: _convertToFieldConfigs(telaConfig.fields),\r\n-          idFieldName: telaConfig.idFieldName,\r\n-          dateFieldName: telaConfig.dateFieldName,\r\n-          storageKey: widget.storageKey ?? telaConfig.storageKey ?? 'dynamic_grid_${telaConfig.nome}',\r\n-          onItemTap: widget.onItemTap,\r\n-          detailScreenBuilder: widget.detailScreenBuilder,\r\n-          extraParams: widget.extraParams,\r\n-          enableSearch: telaConfig.enableSearch,\r\n-          enableDebugMode: telaConfig.enableDebugMode,\r\n-          useUserBannerAppBar: telaConfig.useUserBannerAppBar,\r\n-          onUserBannerTapped: widget.onUserBannerTapped,\r\n-          onBannerRefresh: widget.onBannerRefresh,\r\n-          additionalFormData: widget.additionalFormData,\r\n-          dynamicAdditionalFormData: widget.dynamicAdditionalFormData,\r\n-        );\r\n-      },\r\n-    );\r\n-  }\r\n-}\n\\ No newline at end of file\n+typedef SecurityCheck = bool Function(String permission);\r\n+typedef OnItemTap = void Function(\r\n+    Map<String, dynamic> item, BuildContext context);\r\n+typedef FromJson<T> = T Function(Map<String, dynamic> json);\r\n+typedef ToJson<T> = Map<String, dynamic> Function(T object);\r\n"
                },
                {
                    "date": 1760906189397,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,11 +1,14 @@\n import 'package:flutter/material.dart';\r\n-import 'package:task_manager_flutter/data/customization/generic_grid_card.dart';\r\n+import 'package:task_manager_flutter/data/customization/generic_grid_card_1_1.dart';\r\n import 'package:task_manager_flutter/data/services/network_caller.dart';\r\n import 'package:task_manager_flutter/data/services/tela_caller.dart';\r\n \r\n import '../models/telas_model.dart';\r\n \r\n+typedef SecurityCheck = bool Function(String permission);\r\n+typedef OnItemTap = void Function(Map<String, dynamic> item, BuildContext context);\r\n+\r\n class DynamicGridDynamicScreen extends StatefulWidget {\r\n   final String telaNome;\r\n   final SecurityCheck hasPermission;\r\n   final String? storageKey;\r\n@@ -48,19 +51,11 @@\n     _telaFuture = _loadTelaConfig();\r\n   }\r\n \r\n   Future<TelaConfig> _loadTelaConfig() async {\r\n-    print('🔄 Carregando configuração da tela: ${widget.telaNome}');\r\n-\r\n     final tela = await _telaService.getTelaFromCache(widget.telaNome);\r\n-\r\n-    if (tela != null) {\r\n-      print('✅ Configuração carregada com sucesso');\r\n-      return tela;\r\n-    }\r\n-\r\n-    throw Exception(\r\n-        'Tela ${widget.telaNome} não encontrada no cache nem na API');\r\n+    if (tela != null) return tela;\r\n+    throw Exception('Tela ${widget.telaNome} não encontrada no cache nem na API');\r\n   }\r\n \r\n   List<FieldConfig> _convertToFieldConfigs(List<TelaField> fields,\r\n       {bool forInsert = true, bool forUpdate = false}) {\r\n@@ -72,32 +67,25 @@\n       List<Map<String, dynamic>>? dropdownOptions;\r\n       if (field.dropdownOptions.isNotEmpty) {\r\n         final uniqueOptions = <String, Map<String, dynamic>>{};\r\n         for (final option in field.dropdownOptions) {\r\n-          final value = option.optionValue.toString() ?? '';\r\n+          final value = option.optionValue?.toString() ?? '';\r\n           if (value.isNotEmpty && !uniqueOptions.containsKey(value)) {\r\n             uniqueOptions[value] = {\r\n               'value': option.optionValue,\r\n-              'label':\r\n-                  option.optionLabel ?? option.optionValue.toString() ?? '',\r\n+              'label': option.optionLabel ?? option.optionValue?.toString() ?? '',\r\n             };\r\n           }\r\n         }\r\n         dropdownOptions = uniqueOptions.values.toList();\r\n-\r\n-        print(\r\n-            'Dropdown ${field.fieldName}: ${dropdownOptions.length} opções únicas');\r\n       }\r\n \r\n       dynamic selectedValue = field.dropdownSelectedValue;\r\n       if (selectedValue != null && dropdownOptions != null) {\r\n-        final valueExists = dropdownOptions.any((option) =>\r\n-            option['value']?.toString() == selectedValue?.toString());\r\n-        if (!valueExists) {\r\n-          print(\r\n-              '⚠️ Valor selecionado $selectedValue não existe nas opções para ${field.fieldName}');\r\n-          selectedValue = null;\r\n-        }\r\n+        final exists = dropdownOptions.any(\r\n+          (o) => o['value']?.toString() == selectedValue.toString(),\r\n+        );\r\n+        if (!exists) selectedValue = null;\r\n       }\r\n \r\n       return FieldConfig(\r\n         label: field.label,\r\n@@ -146,63 +134,46 @@\n         final response = await NetworkCaller().getRequest(endpoint);\r\n         if (response.isSuccess && response.body != null) {\r\n           final data = response.body!['data'] ?? response.body!;\r\n           if (data is List) {\r\n-            final List<Map<String, dynamic>> items =\r\n-                data.cast<Map<String, dynamic>>();\r\n-\r\n-            final uniqueItems = <String, Map<String, dynamic>>{};\r\n-            for (final item in items) {\r\n-              final value =\r\n-                  item['value']?.toString() ?? item['id']?.toString() ?? '';\r\n-              if (value.isNotEmpty && !uniqueItems.containsKey(value)) {\r\n-                uniqueItems[value] = {\r\n-                  'value': item['value'] ?? item['id'],\r\n-                  'label': item['label'] ??\r\n-                      item['name'] ??\r\n-                      item['value']?.toString() ??\r\n-                      'Sem label',\r\n-                };\r\n-              }\r\n+            final items = data.cast<Map<String, dynamic>>();\r\n+            final unique = <String, Map<String, dynamic>>{};\r\n+            for (final it in items) {\r\n+              final v = it['value']?.toString() ?? it['id']?.toString() ?? '';\r\n+              if (v.isEmpty || unique.containsKey(v)) continue;\r\n+              unique[v] = {\r\n+                'value': it['value'] ?? it['id'],\r\n+                'label': it['label'] ??\r\n+                    it['name'] ??\r\n+                    it['value']?.toString() ??\r\n+                    'Sem label',\r\n+              };\r\n             }\r\n-\r\n-            print(\r\n-                '✅ Dropdown carregado: ${uniqueItems.length} itens únicos de $endpoint');\r\n-            return uniqueItems.values.toList();\r\n+            return unique.values.toList();\r\n           }\r\n         }\r\n-        print('⚠️ Dropdown vazio ou erro na resposta de $endpoint');\r\n         return [];\r\n-      } catch (e) {\r\n-        print('❌ Erro ao carregar dropdown de $endpoint: $e');\r\n+      } catch (_) {\r\n         return [];\r\n       }\r\n     };\r\n   }\r\n \r\n   String? Function(String?)? _createValidator(TelaField field) {\r\n     if (!field.isRequired) return null;\r\n-\r\n     return (value) {\r\n-      if (value == null || value.isEmpty) {\r\n-        return '${field.label} é obrigatório';\r\n-      }\r\n-\r\n+      if (value == null || value.isEmpty) return '${field.label} é obrigatório';\r\n       switch (field.fieldType) {\r\n         case FieldType.email:\r\n           if (!RegExp(r'^[\\w\\-.]+@([\\w\\-]+\\.)+[\\w\\-]{2,4}$').hasMatch(value)) {\r\n             return 'Email inválido';\r\n           }\r\n           break;\r\n         case FieldType.cpf:\r\n-          if (!_validateCPF(value)) {\r\n-            return 'CPF inválido';\r\n-          }\r\n+          if (!_validateCPF(value)) return 'CPF inválido';\r\n           break;\r\n         case FieldType.cnpj:\r\n-          if (!_validateCNPJ(value)) {\r\n-            return 'CNPJ inválido';\r\n-          }\r\n+          if (!_validateCNPJ(value)) return 'CNPJ inválido';\r\n           break;\r\n         case FieldType.phone:\r\n           if (!RegExp(r'^\\(\\d{2}\\)\\s\\d{4,5}-\\d{4}$').hasMatch(value)) {\r\n             return 'Telefone inválido';\r\n@@ -210,132 +181,76 @@\n           break;\r\n         default:\r\n           break;\r\n       }\r\n-\r\n       return null;\r\n     };\r\n   }\r\n \r\n-  bool _validateCPF(String cpf) {\r\n-    final cleaned = cpf.replaceAll(RegExp(r'[^\\d]'), '');\r\n-    return cleaned.length == 11;\r\n-  }\r\n+  bool _validateCPF(String cpf) =>\r\n+      cpf.replaceAll(RegExp(r'[^\\d]'), '').length == 11;\r\n \r\n-  bool _validateCNPJ(String cnpj) {\r\n-    final cleaned = cnpj.replaceAll(RegExp(r'[^\\d]'), '');\r\n-    return cleaned.length == 14;\r\n-  }\r\n+  bool _validateCNPJ(String cnpj) =>\r\n+      cnpj.replaceAll(RegExp(r'[^\\d]'), '').length == 14;\r\n \r\n   @override\r\n   Widget build(BuildContext context) {\r\n     return FutureBuilder<TelaConfig>(\r\n       future: _telaFuture,\r\n-      builder: (context, snapshot) {\r\n-        if (snapshot.connectionState == ConnectionState.waiting) {\r\n-          return const Scaffold(\r\n-            body: Center(child: CircularProgressIndicator()),\r\n-          );\r\n+      builder: (context, s) {\r\n+        if (s.connectionState == ConnectionState.waiting) {\r\n+          return const Scaffold(body: Center(child: CircularProgressIndicator()));\r\n         }\r\n-\r\n-        if (snapshot.hasError) {\r\n+        if (s.hasError) {\r\n           return Scaffold(\r\n             appBar: AppBar(title: const Text('Erro')),\r\n             body: Center(\r\n               child: Column(\r\n                 mainAxisAlignment: MainAxisAlignment.center,\r\n                 children: [\r\n                   const Icon(Icons.error_outline, size: 64, color: Colors.red),\r\n                   const SizedBox(height: 16),\r\n-                  Text(\r\n-                    'Erro ao carregar tela: ${snapshot.error}',\r\n-                    textAlign: TextAlign.center,\r\n-                    style: const TextStyle(fontSize: 16),\r\n-                  ),\r\n+                  Text('Erro ao carregar tela: ${s.error}'),\r\n                   const SizedBox(height: 16),\r\n                   ElevatedButton(\r\n-                    onPressed: () => setState(() {\r\n-                      _telaFuture = _loadTelaConfig();\r\n-                    }),\r\n-                    child: const Text('Tentar Novamente'),\r\n+                    onPressed: () => setState(() => _telaFuture = _loadTelaConfig()),\r\n+                    child: const Text('Tentar novamente'),\r\n                   ),\r\n                 ],\r\n               ),\r\n             ),\r\n           );\r\n         }\r\n-\r\n-        if (!snapshot.hasData) {\r\n-          return const Scaffold(\r\n-            body: Center(child: Text('Tela não encontrada')),\r\n-          );\r\n+        if (!s.hasData) {\r\n+          return const Scaffold(body: Center(child: Text('Tela não encontrada')));\r\n         }\r\n \r\n-        final telaConfig = snapshot.data!;\r\n-\r\n-        _debugDropdownFields(telaConfig.fields);\r\n-\r\n+        final tela = s.data!;\r\n         return GenericMobileGridScreen(\r\n-          title: telaConfig.titulo,\r\n-          fetchEndpoint: telaConfig.fetchEndpoint,\r\n-          createEndpoint: telaConfig.createEndpoint,\r\n-          updateEndpoint: telaConfig.updateEndpoint,\r\n-          deleteEndpoint: telaConfig.deleteEndpoint,\r\n-          fromJson: (json) => json,\r\n-          toJson: (item) => item,\r\n+          title: tela.titulo,\r\n+          fetchEndpoint: tela.fetchEndpoint,\r\n+          createEndpoint: tela.createEndpoint,\r\n+          updateEndpoint: tela.updateEndpoint,\r\n+          deleteEndpoint: tela.deleteEndpoint,\r\n           hasPermission: widget.hasPermission,\r\n-          fieldConfigs: _convertToFieldConfigs(telaConfig.fields),\r\n-          idFieldName: telaConfig.idFieldName,\r\n-          dateFieldName: telaConfig.dateFieldName,\r\n-          storageKey: widget.storageKey ??\r\n-              telaConfig.storageKey ??\r\n-              'dynamic_grid_${telaConfig.nome}',\r\n+          fieldConfigs: _convertToFieldConfigs(tela.fields),\r\n+          idFieldName: tela.idFieldName,\r\n+          dateFieldName: tela.dateFieldName,\r\n+          storageKey: widget.storageKey ?? tela.storageKey ?? 'dynamic_grid_${tela.nome}',\r\n           onItemTap: widget.onItemTap,\r\n           detailScreenBuilder: widget.detailScreenBuilder,\r\n           extraParams: widget.extraParams,\r\n-          enableSearch: telaConfig.enableSearch,\r\n-          enableDebugMode: telaConfig.enableDebugMode,\r\n-          useUserBannerAppBar: telaConfig.useUserBannerAppBar,\r\n+          enableSearch: tela.enableSearch,\r\n+          enableDebugMode: tela.enableDebugMode,\r\n+          useUserBannerAppBar: tela.useUserBannerAppBar,\r\n           onUserBannerTapped: widget.onUserBannerTapped,\r\n           onBannerRefresh: widget.onBannerRefresh,\r\n           additionalFormData: widget.additionalFormData,\r\n           dynamicAdditionalFormData: widget.dynamicAdditionalFormData,\r\n+          // Se seus endpoints multipart forem relativos, informe o base (opcional):\r\n+          // baseUrlForMultipart: 'https://seu-dominio.com',\r\n+          // authHeadersProvider: () async => await AuthService().authHeaders(),\r\n         );\r\n       },\r\n     );\r\n   }\r\n-\r\n-  void _debugDropdownFields(List<TelaField> fields) {\r\n-    for (final field in fields) {\r\n-      if (field.dropdownOptions.isNotEmpty || field.dropdownEndpoint != null) {\r\n-        print('🔍 Dropdown Field: ${field.fieldName}');\r\n-        print('   - Tipo: ${field.fieldType}');\r\n-        print('   - Opções locais: ${field.dropdownOptions.length}');\r\n-        print('   - Endpoint: ${field.dropdownEndpoint}');\r\n-        print('   - Valor selecionado: ${field.dropdownSelectedValue}');\r\n-\r\n-        if (field.dropdownOptions.isNotEmpty) {\r\n-          final values =\r\n-              field.dropdownOptions.map((o) => o.optionValue).toList();\r\n-          print('   - Valores disponíveis: $values');\r\n-\r\n-          final valueSet = <dynamic>{};\r\n-          final duplicates = <dynamic>[];\r\n-          for (final value in values) {\r\n-            if (!valueSet.add(value)) {\r\n-              duplicates.add(value);\r\n-            }\r\n-          }\r\n-          if (duplicates.isNotEmpty) {\r\n-            print('   ⚠️ VALORES DUPLICADOS: $duplicates');\r\n-          }\r\n-        }\r\n-      }\r\n-    }\r\n-  }\r\n }\r\n-\r\n-typedef SecurityCheck = bool Function(String permission);\r\n-typedef OnItemTap = void Function(\r\n-    Map<String, dynamic> item, BuildContext context);\r\n-typedef FromJson<T> = T Function(Map<String, dynamic> json);\r\n-typedef ToJson<T> = Map<String, dynamic> Function(T object);\r\n"
                }
            ],
            "date": 1759781203371,
            "name": "Commit-0",
            "content": "import 'package:file_picker/file_picker.dart';\r\nimport 'package:flutter/material.dart';\r\nimport 'package:intl/intl.dart';\r\nimport 'package:shared_preferences/shared_preferences.dart';\r\nimport 'package:task_manager_flutter/data/models/network_response.dart';\r\nimport 'package:task_manager_flutter/data/services/network_caller.dart';\r\nimport 'package:task_manager_flutter/data/services/upload_file_caller.dart';\r\nimport 'package:task_manager_flutter/ui/widgets/user_banners.dart';\r\n// ==============================================\r\n// MOBILE GRID SCREEN - MATERIAL DESIGN 3 COMPLETO\r\n// ==============================================\r\n\r\nclass GridColors {\r\n  static const Color primary = Color(0xFF93070A);\r\n  static const Color primaryDark = Color(0xFF6A0507);\r\n  static const Color primaryLight = Color(0xFFB84042);\r\n  static const Color secondary = Color(0xFF005826);\r\n  static const Color secondaryLight = Color(0xFF2E7D32);\r\n  static const Color secondaryDark = Color(0xFF003D1A);\r\n  static const Color textPrimary = Color(0xFFFFFFFF);\r\n  static const Color textSecondary = Color(0xFF000000);\r\n  static const Color link = Color(0xFFFF0000);\r\n  static const Color inputBackground = Color(0xFFFFFFFF);\r\n  static const Color inputBorder = Color(0xFF93070A);\r\n  static const Color buttonBackground = Color(0xFF93070A);\r\n  static const Color buttonText = Color(0xFFFFFFFF);\r\n  static const Color background = Color(0xFF005826);\r\n  static const Color card = Color(0xFFFFFFFF);\r\n  static const Color error = Color(0xFFD32F2F);\r\n  static const Color warning = Color(0xFFFFA000);\r\n  static const Color success = Color(0xFF2E7D32);\r\n  static const Color info = Color(0xFF1976D2);\r\n  static const Color divider = Color(0xFFBDBDBD);\r\n  static const Color filterBackground = Color(0xFFEFEFEF);\r\n  static const Color hover = Color(0x1A000000);\r\n  static const Color selectedRow = Color(0xFFE3F2FD);\r\n  static const Color dialogBackground = Color(0xFFFFFFFF);\r\n  static const Color shadow = Color(0x26000000);\r\n}\r\n// screens/dynamic_grid_screen.dart\r\nclass DynamicGridScreen<T> extends StatefulWidget {\r\n  final String telaNome;\r\n  final FromJson<T> fromJson;\r\n  final ToJson<T> toJson;\r\n  final SecurityCheck hasPermission;\r\n  final String? storageKey;\r\n  final OnItemTap<T>? onItemTap;\r\n  final Widget Function(T item)? detailScreenBuilder;\r\n  final Map<String, dynamic>? extraParams;\r\n  final VoidCallback? onUserBannerTapped;\r\n  final VoidCallback? onBannerRefresh;\r\n  final Map<String, dynamic>? additionalFormData;\r\n  final Map<String, dynamic> Function(T? item)? dynamicAdditionalFormData;\r\n\r\n  const DynamicGridScreen({\r\n    super.key,\r\n    required this.telaNome,\r\n    required this.fromJson,\r\n    required this.toJson,\r\n    required this.hasPermission,\r\n    this.storageKey,\r\n    this.onItemTap,\r\n    this.detailScreenBuilder,\r\n    this.extraParams,\r\n    this.onUserBannerTapped,\r\n    this.onBannerRefresh,\r\n    this.additionalFormData,\r\n    this.dynamicAdditionalFormData,\r\n  });\r\n\r\n  @override\r\n  State<DynamicGridScreen<T>> createState() => _DynamicGridScreenState<T>();\r\n}\r\n\r\nclass _DynamicGridScreenState<T> extends State<DynamicGridScreen<T>> {\r\n  late Future<TelaConfig> _telaFuture;\r\n  final TelaService _telaService = TelaService(\r\n    networkCaller: NetworkCaller(),\r\n    prefs: SharedPreferences.getInstance() as SharedPreferences,\r\n  );\r\n\r\n  @override\r\n  void initState() {\r\n    super.initState();\r\n    _telaFuture = _loadTelaConfig();\r\n  }\r\n\r\n  Future<TelaConfig> _loadTelaConfig() async {\r\n    // Tenta carregar do cache primeiro\r\n    final cachedTela = await _telaService.getTelaFromCache(widget.telaNome);\r\n    if (cachedTela != null) {\r\n      return cachedTela;\r\n    }\r\n\r\n    // Se não encontrou no cache, busca da API\r\n    final tela = await _telaService.getTelaByNome(widget.telaNome);\r\n    if (tela != null) {\r\n      await _telaService.saveTelaToCache(widget.telaNome, tela);\r\n      return tela;\r\n    }\r\n\r\n    throw Exception('Tela ${widget.telaNome} não encontrada');\r\n  }\r\n\r\n  List<FieldConfig> _convertToFieldConfigs(List<TelaField> fields, {bool forInsert = true, bool forUpdate = false}) {\r\n    return fields.where((field) {\r\n      if (forInsert && !field.showInInsert) return false;\r\n      if (forUpdate && !field.showInUpdate) return false;\r\n      return field.isInForm;\r\n    }).map((field) {\r\n      return FieldConfig(\r\n        label: field.label,\r\n        fieldName: field.fieldName,\r\n        displayFieldName: field.displayFieldName,\r\n        isFilterable: field.isFilterable,\r\n        isInForm: field.isInForm,\r\n        flex: field.flex,\r\n        maxLines: field.maxLines,\r\n        icon: field.iconData,\r\n        isSortable: field.isSortable,\r\n        fieldType: field.fieldType,\r\n        dropdownOptions: field.dropdownOptionsMap,\r\n        dropdownFutureBuilder: field.dropdownEndpoint != null \r\n            ? _createDropdownFutureBuilder(field.dropdownEndpoint!)\r\n            : null,\r\n        dropdownValueField: field.dropdownValueField,\r\n        dropdownDisplayField: field.dropdownDisplayField,\r\n        isRequired: field.isRequired,\r\n        validator: _createValidator(field),\r\n        isVisibleByDefault: field.isVisibleByDefault,\r\n        isFixed: field.isFixed,\r\n        enabled: field.enabled,\r\n        defaultValue: field.defaultValue,\r\n        fileConfig: field.fieldType == FieldType.file ? field.fileConfig : null,\r\n        dropdownSelectedValue: field.dropdownSelectedValue,\r\n        showInCard: field.showInCard,\r\n        firstDate: field.firstDate,\r\n        lastDate: field.lastDate,\r\n        dateFormat: field.dateFormat,\r\n      );\r\n    }).toList();\r\n  }\r\n\r\n  Future<List<Map<String, dynamic>>> Function()? _createDropdownFutureBuilder(String endpoint) {\r\n    return () async {\r\n      try {\r\n        final response = await NetworkCaller().getRequest(endpoint);\r\n        if (response.isSuccess && response.body != null) {\r\n          final data = response.body!['data'] ?? response.body!;\r\n          return (data as List).cast<Map<String, dynamic>>();\r\n        }\r\n        return [];\r\n      } catch (e) {\r\n        print('Erro ao carregar dropdown: $e');\r\n        return [];\r\n      }\r\n    };\r\n  }\r\n\r\n  String? Function(String?)? _createValidator(TelaField field) {\r\n    if (!field.isRequired) return null;\r\n\r\n    return (value) {\r\n      if (value == null || value.isEmpty) {\r\n        return '${field.label} é obrigatório';\r\n      }\r\n      \r\n      // Validações específicas por tipo\r\n      switch (field.fieldType) {\r\n        case FieldType.email:\r\n          if (!RegExp(r'^[\\w-\\.]+@([\\w-]+\\.)+[\\w-]{2,4}$').hasMatch(value)) {\r\n            return 'Email inválido';\r\n          }\r\n          break;\r\n        case FieldType.cpf:\r\n          if (!_validateCPF(value)) {\r\n            return 'CPF inválido';\r\n          }\r\n          break;\r\n        case FieldType.cnpj:\r\n          if (!_validateCNPJ(value)) {\r\n            return 'CNPJ inválido';\r\n          }\r\n          break;\r\n        case FieldType.phone:\r\n          if (!RegExp(r'^\\(\\d{2}\\)\\s\\d{4,5}-\\d{4}$').hasMatch(value)) {\r\n            return 'Telefone inválido';\r\n          }\r\n          break;\r\n        default:\r\n          break;\r\n      }\r\n      \r\n      return null;\r\n    };\r\n  }\r\n\r\n  bool _validateCPF(String cpf) {\r\n    // Implementação da validação de CPF\r\n    return true;\r\n  }\r\n\r\n  bool _validateCNPJ(String cnpj) {\r\n    // Implementação da validação de CNPJ\r\n    return true;\r\n  }\r\n\r\n  @override\r\n  Widget build(BuildContext context) {\r\n    return FutureBuilder<TelaConfig>(\r\n      future: _telaFuture,\r\n      builder: (context, snapshot) {\r\n        if (snapshot.connectionState == ConnectionState.waiting) {\r\n          return const Scaffold(\r\n            body: Center(child: CircularProgressIndicator()),\r\n          );\r\n        }\r\n\r\n        if (snapshot.hasError) {\r\n          return Scaffold(\r\n            appBar: AppBar(title: const Text('Erro')),\r\n            body: Center(\r\n              child: Column(\r\n                mainAxisAlignment: MainAxisAlignment.center,\r\n                children: [\r\n                  const Icon(Icons.error_outline, size: 64, color: Colors.red),\r\n                  const SizedBox(height: 16),\r\n                  Text(\r\n                    'Erro ao carregar tela: ${snapshot.error}',\r\n                    textAlign: TextAlign.center,\r\n                    style: const TextStyle(fontSize: 16),\r\n                  ),\r\n                  const SizedBox(height: 16),\r\n                  ElevatedButton(\r\n                    onPressed: () => setState(() {\r\n                      _telaFuture = _loadTelaConfig();\r\n                    }),\r\n                    child: const Text('Tentar Novamente'),\r\n                  ),\r\n                ],\r\n              ),\r\n            ),\r\n          );\r\n        }\r\n\r\n        if (!snapshot.hasData) {\r\n          return const Scaffold(\r\n            body: Center(child: Text('Tela não encontrada')),\r\n          );\r\n        }\r\n\r\n        final telaConfig = snapshot.data!;\r\n\r\n        return GenericMobileGridScreen<T>(\r\n          title: telaConfig.titulo,\r\n          fetchEndpoint: telaConfig.fetchEndpoint,\r\n          createEndpoint: telaConfig.createEndpoint,\r\n          updateEndpoint: telaConfig.updateEndpoint,\r\n          deleteEndpoint: telaConfig.deleteEndpoint,\r\n          fromJson: widget.fromJson,\r\n          toJson: widget.toJson,\r\n          hasPermission: widget.hasPermission,\r\n          fieldConfigs: _convertToFieldConfigs(telaConfig.fields),\r\n          idFieldName: telaConfig.idFieldName,\r\n          dateFieldName: telaConfig.dateFieldName,\r\n          storageKey: widget.storageKey ?? telaConfig.storageKey ?? 'dynamic_grid_${telaConfig.nome}',\r\n          onItemTap: widget.onItemTap,\r\n          detailScreenBuilder: widget.detailScreenBuilder,\r\n          extraParams: widget.extraParams,\r\n          enableSearch: telaConfig.enableSearch,\r\n          enableDebugMode: telaConfig.enableDebugMode,\r\n          useUserBannerAppBar: telaConfig.useUserBannerAppBar,\r\n          onUserBannerTapped: widget.onUserBannerTapped,\r\n          onBannerRefresh: widget.onBannerRefresh,\r\n          additionalFormData: widget.additionalFormData,\r\n          dynamicAdditionalFormData: widget.dynamicAdditionalFormData,\r\n        );\r\n      },\r\n    );\r\n  }\r\n}"
        }
    ]
}